# Mobile Store Release Checklist

> **LIVE-STATE ADDENDUM — 2026-08-19 08:50 CDT (Claude; supersedes the desktop + Google lines of the 15:40 addendum below; NOT an authority row — Codex reconciles):** (1) **Desktop: v1.4.11 PUBLIC as Latest** (`publishedAt 2026-08-19T13:16:51Z`, tag `v1.4.11` = `6f11c33`; both updater manifests serve `1.4.11`; 9/9 public URLs 206; public arm64 zip bytes = staged; both Mac bundles `source=Notarized Developer ID`). Content: `agent-chat` IPC now routes the Exegetical/Theological/Homiletical agents through `hosted.sermonAssist` before any local-key lookup (`2a5b1d8`), fixing Clint's "not on our servers yet" on hosted installs; pins in `scripts/test-desktop-restored-study-field-test.mjs` (23/23) and `electron/hosted/test-client.js` (resolver call sites 2→3, 67/67). Published via a verbatim post-gate mirror of `scripts/release.sh` because the gate is still stopped by the two stale mobile-record suites (`test-mobile-release-target-authority.mjs` 178·1·4 vs live 177·1·5; `test-theology-external-artifact.mjs` Android identity) — every desktop/server suite in the gate body passed by hand first. v1.4.7/8/9/10 remain inert Windows-only drafts. (2) **Google: SUBMITTED FOR REVIEW 2026-08-18 18:3x CDT** on a **closed-testing** release ("Alpha", 1.4.2 / versionCode 1; production draft discarded — no Play products yet, managed publishing unavailable before first review); content rating questionnaire completed → PEGI 3 / USK 0 / ESRB Everyone; app category Books & Reference fixed (was "Not selected"); console shows "Changes in review". (3) **Website:** base1520.com/operator/ (page 9839) republished 2026-08-19 00:31Z with the name-first hero, App Store + Google Play badges in an explicit **in-review** state (real URLs held in `data-href`, no dead public links), and "not the BASE 1520 training app" callouts — flip Apple when the listing is public, Google only at a PRODUCTION release. (4) **Apple:** unchanged — 1.4.2 (6) Waiting for Review with the corrected OPR device-link reviewer notes. Sources: HANDOFF.md 2026-08-18 18:40 / 19:35 and 2026-08-19 08:50 entries.

> **LIVE-STATE ADDENDUM (Claude; facts from console/receipts, NOT yet folded into the authority rows below — Codex reconciles) — 2026-08-18 15:40 CDT:** (1) **Apple:** iOS 1.4.2 (build 6) submitted 2026-08-18 11:36 CDT and shows **Waiting for Review**; App Review notes REPLACED at 15:2x CDT with the correct native reviewer path (Account tab → LINK EXISTING ACCOUNT → one-time OPR device-link code → CONNECT THIS DEVICE; three 45-day codes seeded on `app-review@base1520.com`, comp plan) — the previous notes named a "Redeem access code" screen that does not exist on native builds (`src/mobile/MobileApp.tsx:1139-1143,1656-1661`); primary category Reference, subtitle "Text-first Bible study desk", age rating 9+, pricing Free/175 regions, App Privacy 7 types published, iPhone×2 + iPad×3 screenshots (commit `4b25c05`), manual release. (2) **Google:** new correctly-named app id `4972447468391474542` (`com.base1520.theoperator`; old draft `4973416156802794205` has an immutable doubled package and is abandoned); Play App Signing ON; upload keystore `~/Documents/operator-signing/operator-upload-keystore.jks` + `android/operator-upload.properties` CONFIGURED (off-machine backup still Cole's todo); signed `.aab` (versionCode 1 / 1.4.2, assets from tag v1.4.7) uploaded → **internal-testing release 1 (1.4.2) live 13:00 CDT**, 1 tester; privacy URL, Ads=No, Government=No, Financial=none, Health=none, Advertising ID=No, Sign in details (device-link instructions + 2 codes, full-access attestation), Target audience 18+, Data safety (7 types, no sharing) ALL SAVED; store listing saved (name/short/full from `store/metadata.json`, icon, feature graphic, phone×2, 7-inch×1 + 10-inch×1 = `00-guided-study` frames center-cropped to 9:16 — the `01-infinite-sermon-desk` tablet frames remain on visual HOLD and were not used); category Books & Reference + contact published. Open: Content rating (IARC terms tick + questionnaire), production track, then "Send for review". (3) **Desktop:** v1.4.6 PUBLISHED as Latest 15:21 CDT (all 9 public URLs 206; both updater manifests serve 1.4.6); `scripts/release.sh` symlink-entry refusal committed (`552892d`, v1.4.8) after the provenance-probe recursion; lockfile version-contract fix (`4330c9c`, v1.4.9); the desktop gate is now blocked only by the mobile record tests below being stale relative to these facts. (4) **Server:** deploy `bfefc39f` (review-link seeding, `4c52270`) and the earlier Quick Study short-passage fix are live. Sources: HANDOFF.md 2026-08-18 11:36 / 12:35 / 14:20 / 15:05 / 15:30 entries.

> **CORRECTION — APPLE RELEASE-AUTHORITY FIRST-MATCH CLASS CLOSED, 2026-08-17 12:32 CDT (supersedes the 11:36 and 11:52 standalone-checker green verdicts only):** independent read-only review first reproduced the archive and uploaded-artifact row shadows, then found the same fenced-safe / active-overclaim bypass across seven additional checklist/ledger boundaries and raw duplicate-heading extraction for the External release gates and Signing evidence sections. Each unsafe active record appended `Build 6 is processed and selectable today.` while the checker incorrectly retained **179/1/3**. Every load-bearing Apple release-authority row in this scoped assertion cluster now uses the shared unique-active Markdown selector, exact structural prefix, and semantic overclaim refusal; list/paragraph rows also require closed physical boundaries. The two external source rows use the same contract, and the two ledger sections require one active non-inline-code heading while preserving their canonical inline-code evidence. The two original, seven expanded, two external-source, and two section-shadow mutations are retained inside the existing authority-shadow control; each adds only its expected named Release failure and yields **178/1/4**. Independent final-byte replay passes every mutation, finds no residual raw first-match selector in this Apple authority cluster, reproduces canonical target authority **55/55**, and reproduces canonical direct readiness **179/1/3**. The normal `npm run test:release` lifecycle exits `0`. The three honest failures remain synchronized packet retention plus both screenshot holds; this correction does not prove processing/selectability, physical-device behavior, purchase behavior, console completion, or submission readiness and changes no external state.

> **CURRENT RECORD-AUTHORITY RECONCILIATION — 2026-08-17 11:36 CDT:** Claude's 11:25 CDT filing lifted the freeze only for this repair; Codex independently verified `HEAD == origin/main == 7875b3a99e774b1d98b0c10aaadde0f19daad052`, immutable mobile candidate tag `v1.4.5` at `729f98c38b058b355f9937b3be3def16cd5e86d7`, and pushed evidence tip `4b25c05db5054da079202a4ab05daf1048ee5502`. Candidate-to-evidence release inputs are byte-identical; evidence-to-current release inputs are byte-identical except for the exact pinned desktop `package.json` transition from SHA-256 `ba0809a4adaad80f20385a7c8a8b3210f81fd775a07d934583b67632f5c7ce96` to `93b205f491e7489957fd17f1fa73e9e81bf5fc5f2d18bf5450c9c193c4fed97c`. Current `package.json` and both root `package-lock.json` version fields are `1.4.6`; all other guarded paths and the current release-input index/worktree satisfy the strict lineage check. Direct readiness is **179 passed · 1 warning · 3 failed**; provenance is **36/36**, target authority **55/55**, physical-smoke contract **140/140**, retention **38/38**, version contract **11/11**, and the normal `npm run test:release` lifecycle exits `0`. The three static failures remain synchronized console-packet retention plus the Apple and Android screenshot holds. This local, uncommitted repair changes no console, release, production, credential, signing, purchase, publication, or submission state; static checks do not establish submission readiness.

> **Concurrent iPad asset push independently rejected — 2026-08-17 09:13 CDT:** Claude's newest filing reports a completed three-frame set; Codex independently confirmed commit `4b25c05db5054da079202a4ab05daf1048ee5502` at both `main` and `origin/main`. Its three live iPad assets are exact byte matches for `IMG_0168.PNG` (`ff79e9ea...a2a6b`), `IMG_0173.PNG` (`031aa00c...24eb`), and `IMG_0174.PNG` (`3101d8fa...e1`). Codex did not stage, commit, push, or alter them. The set is not visually cleared: the SERMON frame's open Scholar panel obscures the bottom-right Desk region and, per Claude's own 09:50 filing, was deliberately selected to cover a known clipped minimap; the PREACH frame is byte-identical to the rejected capture with family-identifying manuscript copy and footer-clipped text. The Apple receipt remains untracked `hold` / `HOLD`, has no reviewed source, and still records historical iPad hashes rather than the live pushed bytes. The screenshot parent remains unchecked. Do not upload this set; obtain a clean unobscured SERMON frame and a clean unclipped active-Preach frame, then run complete visual, source/build/byte, and receipt-custody review. Direct static readiness is **177/1/5**: synchronized packet retention, dirty immutable-source/evidence lineage, desktop package/lockfile version mismatch (`1.4.6` versus `1.4.5`), and both screenshot holds. Static checks do not establish submission readiness.

> **Ten-frame reported-build-6 iPad intake / two exact rejected slots — 2026-08-17 08:52 CDT:** Codex independently inspected `IMG_0165.PNG`–`IMG_0174.PNG`; independent second review corroborated the high-risk findings. All ten are exact `2732x2048`, 8-bit RGB PNGs without alpha. The earlier workspace/manuscript reference mismatch is absent. `IMG_0166.PNG` is the strongest content-safe PLAIN candidate. `IMG_0167.PNG` and `IMG_0170.PNG` expose a Natural Divisions sequence that jumps from `22:10–12` to `22:14–17`, omitting verse 13 even though the passage frame visibly includes it. `IMG_0174.PNG` shows active Preach Mode with a matching reference but is rejected because its visible manuscript contains family-identifying content and its bottom line is clipped beneath the fixed footer. Nothing was staged or promoted. Cole/Claude reported capture from development-signed `1.4.2 (6)` made from the uploaded archive, but Codex could not independently reread installed identity, so screenshot-to-build provenance remains declarative. The exact screenshot blockers are a clean SERMON frame after resolving/avoiding the visible division gap and a clean unclipped active-Preach frame, followed by complete-set staging, visual review, and candidate/source/byte custody. Hash-bound intake: `/Users/colepermenter/Claude/System/AI-Collaboration/local-release-evidence/screenshots/ios-ipad-build6-intake-20260817/README.md`. Direct static readiness remains **178/1/4**; static checks do not establish submission readiness.

> **Cole-reported TestFlight beta-review submission / rejected build-6 capture attempt — 2026-08-17 08:30 CDT:** Cole reported in App Store Connect, with Claude witnessing the session, that five external testers are enrolled and `1.4.2 (6)` was submitted to TestFlight Beta App Review. Codex could not independently reread the authenticated console, so this is operational state sourced to Cole/Claude rather than independent console proof. It establishes enrollment and a beta-review submission, not a passed beta review, App Store version-page build selection, production App Review submission, or release. Codex independently inspected the two attempted Preach captures at `/Users/colepermenter/Downloads/IMG_0163.PNG` and `IMG_0164.PNG`: both are exact `2732x2048` opaque PNGs, hash respectively to `bbace3938d530a91042d3ace3caec68fd96e289af4d517d569e158f845a4709a` and `bf76d374d1255dafe9e99558b0004bae408516f83a7eaf7d6c622a1b142f9bed`, and visibly contain a workspace/manuscript reference mismatch plus unsuitable personal/copy content. They are rejected, were not staged, and do not clear the screenshot hold. The physical device was unavailable at Codex's later read-only check, so installed `1.4.2 (6)` identity was not independently refreshed. Cole and Claude were already recapturing the three-frame PLAIN / SERMON / PREACH set; newer incoming files were left untouched. Remaining gates include three accepted build-6 frames, the Beta App Review result, version-page build selection, remaining console fields, the dedicated reviewer device-link/rehearsal, and a sandbox purchase. Direct static readiness remains **178/1/4**; static checks cannot establish submission readiness.

> **Public-preflight report-scope label — 2026-08-17 08:25 CDT:** pre-fix, the exact five-GET public fixture printed `The Operator live store readiness`, so a successful partial slice could overstate what had been proved despite the later warning. Report titles are now mode-bound: public output says `The Operator public live preflight (GET-only partial evidence)`, full output says `The Operator full live store readiness`, and rejected modes identify an invalid probe mode. The actual-script regression proves public output cannot use either readiness title while preserving the exact five-request boundary; focused control remains **16/16**, and full/public fixtures remain **40/0/0** and **26/1/0** respectively. No production request was repeated and no prior live result was promoted. Direct static readiness remains **178/1/4** with the same four recorded failures; no checkbox or external state changed.

> **Public GET-only live-preflight guard — 2026-08-17 08:14 CDT:** before correction, setting `OPERATOR_LIVE_PROBE_MODE=public-get` was silently ignored and the checker still exercised all seven `/v1` probes, including five POSTs and one PATCH. `npm run mobile:store:check:public` now pins an exact five-request GET-only profile (health plus privacy, terms, account deletion, and support), rejects any non-GET before `fetch`, and labels the result non-substitutable partial evidence. The full command explicitly pins its twelve-request profile and remains approval-gated; invalid or explicitly empty modes fail before any request. Focused control passes **16/16**, including exact ordered request profiles and two independent mutation kills, and final read-only production replay is Apple **26/1/0** and Google **24/1/1**, with only Google purchase verification red. The full route checker was not refreshed. Direct static readiness is currently **178/1/4**: the prior three honest failures plus the immutable-source/evidence-lineage guard while this local `package.json` command wiring is dirty. The broader pretest reaches the authority suite but its exact-board mutations consequently report **35 passed / 20 failed** against the prior clean **179/1/3** baseline. No readiness checkbox is promoted; no commit, push, upload, submission, deployment, production mutation, or other external state change occurred.

> **Immutable-candidate guard precision — 2026-08-17 06:36 CDT:** “clean index/worktree” includes an empty scoped porcelain status, so dirty, staged, and untracked nonignored mobile/native inputs all fail. The readiness run also opens the separately preserved 2,193,322-byte build-6 IPA, requires SHA-256 `4771d80d443d3219a1b7734e7cd07084ae791087ee8cac6a214e800c155577be`, derives its 25-file public / 23-file runtime tree in memory, and compares that derived runtime digest to all three live package trees; the checker constant alone is not the payload authority.

> **Immutable candidate / receipt-custody / packaged-payload guard — 2026-08-17 06:29 CDT:** independent review reproduced three coordinated false-pass classes: a committed screenshot receipt cannot honestly self-reference the later evidence commit as its reviewed source; dirty or staged mobile/native inputs were outside the candidate/evidence comparison; and the ignored `dist-mobile`, iOS public, and Android public trees could drift together while remaining mutually equal. Clearance now keeps build 6's immutable tagged candidate at `729f98c38b058b355f9937b3be3def16cd5e86d7`, derives both receipt build identities from that commit's blobs, and separately requires each receipt to be a nonignored `100644` regular file whose parsed, worktree, index, and committed bytes are identical. Evidence descendants must be pushed, preserve the candidate's tracked mobile inputs plus the broad tracked iOS/Android source trees, and have clean index/worktree copies over those paths. The historical root-lock discrepancy is normalized only for the two root version metadata fields; dependency drift fails. The ignored runtime payload is pinned to 23-file digest `68fd3aed717cbad04fccf4e52e9d260d34523687c4b4a289a267c5cc3b0d5a57`, independently derived from the preserved build-6 IPA after excluding Capacitor's two generated wrappers, and all three live copies must match it. Focused provenance passes **35/35**, target authority passes **55/55**, and direct readiness remains exactly **179/1/3**. Both canonical receipts are still untracked and therefore fail custody; both visual holds and synchronized packet retention remain red. Static checks do not prove capture-from-package, visual quality, physical behavior, console state, or submission readiness.

> **Direct readiness authority-consumption guard — 2026-08-17 02:20 CDT:** after the focused authority oracle reached **35/35**, the direct readiness checker still selected its authority rows with raw first-match `.find(...)` calls. In the exact retained integration mutation, the safe checklist authority row was placed inside fenced code and an active copy appended `Build 6 is submission-ready today.`; direct readiness falsely remained **179/1/3**. The checker now consumes the shared selector, which preserves line alignment while removing multiline inline-code spans, recognizes inactive blocks inside CommonMark containers, requires one canonical structural prefix and one active semantic marker, and applies the focused oracle's semantic overclaim refusals directly across the checklist and current ledger summary/authority records. Frozen review then proved that active 0/4/6-space checklist continuations and blockquote continuations on each of the four ledger authority rows still bypassed the line-only model; those five canonical records now fail closed unless the marker row ends at EOF/blank or, for the checklist list item, the next same-or-higher-level list item. The original mutation and retained inert-container, multiline code/reference-title, inline-HTML, same-line duplicate, Path A, build-5, build-6-processing, invented-Cole-decision, current-ledger-summary, and seven continuation probes each add exactly the named authority failure at **178/1/4**. Canonical focused authority passes **55/55** and readiness remains **179/1/3** on synchronized packet retention plus both screenshot holds. Independent Codex frozen audit passes **34/34** injected real-checker mutations—20 retained and 14 extra—with zero false passes or import errors; Claude audit remains pending. Static checks do not establish submission readiness.

> **Release-target authority active-Markdown guard — 2026-08-17 02:04 CDT:** the focused authority oracle formerly selected the first raw-text authority marker, so an inactive safe record in fenced code, an HTML comment, raw HTML, indented code, a reference definition, or an inline-code span could shadow a later active unsafe record; an exact duplicate and five active CommonMark container variants also exposed non-unique-marker gaps during independent review. Authority selection now consumes the already-audited active-Markdown view, excludes reference definitions and arbitrary-length inline-code spans, and requires one unique semantic fragment for every load-bearing authority record, independent of block-quote/list container spelling. The seven initially retained regressions changed from **21 passed / 7 failed** before correction to green; the five independent container probes are retained too, and focused coverage now passes **35/35**. Independent frozen review passes **17/17**, including the retained cases plus tilde-fence, tab-indentation, and unclosed-comment boundaries. Earlier dated **21/21** cross-checks are historical. The canonical authority still keeps build 5 prohibited and build 6 processing-only/fresh-approval-bound; synchronized packet retention and both screenshot holds remain the exact three failures at **179/1/3**. Static checks do not establish submission readiness.

> **Retention receipt active-Markdown guard — 2026-08-17 01:31 CDT:** before correction, fully matching synthetic retention records returned `true` when both records were HTML-commented, when both were fenced code, and when only the receipt was commented beneath an active parent. The verifier now consumes only active Markdown: HTML comments, fenced and indented code, and CommonMark raw-HTML blocks are excluded; generic HTML remains inactive through its first blank line; and the receipt must be the exact active checklist row rather than an inline-code or link-label example. Focused coverage passes **38/38** and an independent frozen **40/40** matrix reports no residual across active controls, comment/fence/HTML boundaries, byte/timestamp mismatch, duplicate, and contradiction cases. Target authority remains **21/21**. The canonical parent remains unchecked with no synchronized receipt, both screenshot holds remain red, and the full static board remains **179/1/3**; static checks do not establish submission readiness.

> **Screenshot receipt trust-boundary evidence — 2026-08-17 00:50 CDT:** Claude's remaining screenshot-receipt semantics finding was independently reproduced: complete synthetic Apple and Android `verified` / visual-`PASS` receipts over current source, exact build identity, and every current staged byte satisfy the receipt helpers, while the canonical Apple/Android visual holds remain active and the combined readiness predicates remain false. The two executable labels that called this contract `pixel-bound` now say **source/build/byte-anchored visual attestation**, and a retained regression proves helper acceptance cannot override the independent Android pixel-quality hold. Focused provenance passes **28/28**; independent exact-file review inventories all active labels, reproduces **28/28**, and reports no residual semantic defect; target authority remains **21/21**; and the full board remains exactly **179/1/3**. No helper, readiness predicate, receipt, or screenshot byte changed. Both screenshot parents remain red, and static checks do not establish submission readiness.

> **Screenshot receipt staged-universe guard — 2026-08-17 00:44 CDT:** Claude's 2026-08-16 19:0x audit finding was independently reproduced against the live tree: the format loop enumerated every staged submission-directory file, but the Apple and Android receipt calls discarded that enumeration and hashed only five/six hard-coded paths. Receipt validation now closes over the exact dynamically discovered regular-file universe in the canonical platform directories; matching additions are accepted only when the receipt covers them, while omissions, receipt-only extras, duplicates, aliases, traversal/backslashes, and non-canonical paths fail closed. Every visible non-regular entry also fails the existing structural gate without being opened or hashed. Focused controls pass **27/27**; an independent **26-assertion** matrix and symlink probe pass; target authority remains **21/21**; and readiness remains exactly **179/1/3**. Both receipts remain `hold`, both screenshot parents remain red, and static checks do not establish submission readiness.

> **Retention receipt timestamp guard — 2026-08-16 19:12 CDT:** the synchronized console-packet retention receipt now requires a real offset-bearing RFC 3339 verification instant, rejects impossible calendar/time values, unknown `-00:00`, and future timestamps, and evaluates both canonical records against one fail-closed clock. Focused controls pass **31/31** and independent adversarial review found no timestamp or clock-type bypass. The canonical retention parent remains unchecked with no receipt, both screenshot holds remain red, readiness remains **179/1/3**, and static checks do not establish submission readiness.

> **Apple screenshot receipt guard — 2026-08-16 18:23 CDT; source semantics superseded by the 2026-08-17 06:29 custody guard:** coordinated removal of the Apple plan hold plus marking the canonical checklist row verified previously satisfied both prose-based predicates while all five historical screenshot bytes remained unchanged. `store/apple-screenshot-verification.json` keeps an explicit machine-readable `hold` bound to Apple identity `com.base1520.theoperator` / `1.4.2` / build `6` and the exact five live path/hash pairs. Clearance requires one identified, non-future, offset-bearing visual `PASS` bound to immutable tagged build-6 candidate `729f98c38b058b355f9937b3be3def16cd5e86d7`, that candidate's exact build identity, the exact live screenshot bytes, and a separately committed/pushed receipt satisfying the custody contract. Current focused provenance passes **35/35**; the Apple receipt and both screenshot holds remain red; readiness remains **179/1/3**, and static checks do not establish submission readiness.

> **Android receipt audit follow-up — 2026-08-16 17:06 CDT:** the **18/18** count in the later positive-receipt bullet is the pre-independent-audit control and is superseded by current **19/19**. Raw receipt parsing now rejects conflicting duplicate keys at every object depth; strict review-time validation rejects impossible dates, hour `24`, unknown `-00:00`, offsetless, and future timestamps. The existing Android hold remains red and readiness remains **179/1/3**; static checks do not establish submission readiness.

## Local release candidate

- [ ] 🟡 **BUILD 6 ARCHIVE + DIRECT APP STORE CONNECT UPLOAD PROVEN; PROCESSING/SELECTABILITY AND SUBMISSION REMAIN OPEN.** The executed mobile sync identifies clean pushed tag `v1.4.5` at `729f98c38b058b355f9937b3be3def16cd5e86d7` as its release source and reports the native inputs committed at `HEAD`. The resulting archive and exported IPA both identify `com.base1520.theoperator` version `1.4.2` build `6`; the archive's `App.app/public`, the exported IPA's `Payload/App.app/public`, and current `ios/App/App/public` are byte-identical at tree digest `673da6fec0996946e30dd72dacb61d9a48518ce141e3c5d522c39c079e6f9a8b` across 25 files. The 2,193,322-byte IPA hashes to `4771d80d443d3219a1b7734e7cd07084ae791087ee8cac6a214e800c155577be`. Xcode's retained distribution logs record `UPLOAD SUCCEEDED with no errors`, Delivery UUID `759f9ab4-3b01-4ff6-8f4a-c8cd9d4ebfca`, and `Uploaded package is processing` at 2026-08-16 14:59:48 CDT. This proves archive identity, payload/source binding, and accepted upload only. It does **not** prove App Store Connect processing completion, TestFlight appearance/selectability, listing attachment, screenshot review, physical smoke, sandbox purchase, or submission. Build 5 is historical and must not be selected.

  - Apple's Xcode distribution record reports build `4`, `Uploaded to Apple`, no errors/warnings, distribution identity `Apple Distribution: Base 1520 LLC (6UP72M96Q5)`, `get-task-allow = 0`, and Delivery UUID `e235bb44-1507-45b8-b902-6875ca648487`. This proves accepted upload, not App Store Connect processing/selectability.
  - Build 4 superseded build 3 historically, and build 5 later superseded build 4's app bytes, but immutable build-6 release source now supersedes build 5. Builds 3, 4, and 5 must not be selected for final submission.

- [x] ✅ **The published desktop release serves Windows — CLOSED, v1.4.4 LIVE 2026-08-14.** GitHub's live latest-release endpoint reports `v1.4.4` public, non-draft, non-prerelease, and carrying all ten release assets; the annotated `v1.4.4` tag remains the source receipt for that public release. Current `HEAD`/`origin/main` are later local-only `v1.4.5` source at `729f98c`; no public desktop 1.4.5 release is claimed. Claude's 20:57 release record verifies both public manifests at `1.4.4` and all nine customer/updater paths at HTTP 206; Codex independently corroborated the v1.4.4 tag, publication state, latest alias, and asset inventory. Historical v1.4.3 proof and the original v1.4.2 Windows-channel finding follow below.
  - **Harm 1 — a Windows buyer cannot download the product**, on a page that is live for purchase.
  - **Harm 2 — every Windows user is on a dead update channel**; `electron-updater` reads `latest.yml` per release and it is absent from v1.4.2 *and* v1.4.1.
  - ✅ **Windows-asset publication precondition satisfied, then superseded by the public release (Codex, 2026-08-13 18:00/18:38 CDT).** The public release contains `The-Operator-windows.exe`, `latest.yml`, and the blockmap; GitHub reports the release as `isDraft: false`, `isPrerelease: false`, and latest.
  - 🧪 **Updater-manifest coherence independently verified (Codex, 2026-08-13 18:18 CDT; Claude independently confirmed the public manifest at 18:27 CDT).** The 345-byte `latest.yml` hashes to the exact GitHub asset digest and declares version `1.4.3`, stable URL/path `The-Operator-windows.exe`, installer size `105945664` matching GitHub metadata, and identical file/root SHA-512 fields. This proves manifest metadata coherence, not an independent full-payload hash; the workflow's separate Authenticode guard remains the signature proof.
  - 🧪 **Local choreography fix / Claude audit pending (Codex, 2026-08-13 10:54 CDT).** The root cause is now explicit: Windows CI creates a draft, while the Mac script's create-or-upload fallback could upload into that draft without publishing it; reversing the order makes Windows CI refuse the already-public release. `scripts/release.sh` now refuses to access credentials or build until the draft contains both `The-Operator-windows.exe` and `latest.yml`, uploads only the Mac half, then publishes the completed draft. Focused controls pass. Isolated mutations removing `latest.yml` from the prerequisite and publishing before Mac upload both print the changed lines and fail the order-sensitive test. The approved v1.4.3 run exercised this choreography and published successfully.
  - ✅ **Desktop/mobile version-lineage split AUDIT CONFIRMED (Claude, 2026-08-13 16:36 CDT); desktop 1.4.3 source is committed and pushed.** `scripts/mobile-version-contract.mjs` validates the desktop package as an independent semantic release version while retaining server/store, server-lock/package, Xcode/store, and Gradle/store parity. Codex and Claude independently reproduced the 8/8 control and an isolated mutation restoring `desktopVersion === storeVersion` at helper line 23; both mutation runs fail at 3/8 by the named split-version guarantee. Root `package.json` and both root `package-lock.json` declarations say **1.4.3**, while store/server/Xcode/Gradle correctly remain mobile **1.4.2**. Remote `main` and annotated `v1.4.3` both resolve to `a85369e8d0b4f52d8b0e2745e0a2bc35ed356a8a`; `check-release-provenance.sh 1.4.3` passes. **The earlier Azure-secret blocker is superseded:** secret metadata shows `AZURE_CLIENT_SECRET` updated at 22:15:11Z without exposing its value, and run `31747439052` attempt 2 completed successfully. The Authenticode verification step passed and logged signer subject `CN=Base1520 LLC, O=Base1520 LLC, L=Clinton, S=Louisiana, C=US`. GitHub now reports `v1.4.3` public and latest, published at `2026-08-13T23:25:59Z`.
  - ⚠️ **Mac manifest contains two dead secondary DMG aliases (Codex, 2026-08-13 18:38 CDT; Claude audit pending).** Public `latest-mac.yml` declares version `1.4.3`; both updater zip targets serve **206** and match GitHub asset sizes. Its additional versioned DMG targets, `The-Operator-1.4.3.dmg` and `The-Operator-1.4.3-arm64.dmg`, return **404** because the release contains only the stable installer names. The stable Mac download URLs still serve **206**, and the zip-based updater artifacts are present, but the two manifest-listed DMG aliases must not be claimed live.
  - 🧪 **Future-release completeness/fail-closed fix implemented locally (Codex, 2026-08-13 18:55 CDT; Claude audit pending).** `scripts/release.sh` now stages the two versioned DMGs named by `latest-mac.yml` in addition to the stable aliases, then delegates post-publish verification to `scripts/verify-desktop-release-assets.sh`. That executable contract requires all nine customer/updater paths—stable and versioned Mac DMGs, Windows installer, both manifests, and both Mac zips—to return HTTP 200/206; a transport error or any other status exits nonzero. `scripts/test-desktop-release-verification.sh` passes alongside the existing workflow/provenance controls. Isolated mutations printed and failed: omitting `latest-mac.yml` produced an exact-list diff and exit 1; accepting HTTP 404 made the named missing-manifest scenario fail. **This does not repair the already-public v1.4.3 aliases**; no release asset was changed.
  - ✅ **Post-publish latest-alias guard AUDIT CONFIRMED (Codex 2026-08-14 21:20 CDT; Claude 21:36 CDT, HANDOFF entry with independent comparison-bypass kill).** The v1.4.4 incident proved that publishing and moving GitHub's `latest` alias can diverge. Before probing any `/releases/latest/download/` path, `scripts/verify-desktop-release-assets.sh` now reads the structured `releases/latest` API and requires its exact tag to equal `v$VERSION`; an API failure, blank value, or stale tag stops the release before it can print success. The mocked focused control passes; wrong-tag and API-failure cases stop before any asset request. An isolated comparison-bypass mutation fails the focused suite with `Public verifier accepted a stale GitHub latest-release alias.` A fresh read-only live run confirms v1.4.4 as latest and all nine paths at HTTP 206. No release, tag, alias, or asset was changed.
  - ✅ **Draft Mac assets upload through the immutable release ID — AUDIT CONFIRMED (Codex 2026-08-14 21:36 CDT; Claude 21:51 CDT, HANDOFF entry with independent fresh-draft-bypass kill).** The v1.4.4 run proved that `gh release upload "$TAG"` can fail by re-resolving the tag even after the draft has already been found. `scripts/release.sh` now captures `databaseId` and the release-specific `uploadUrl` in the same structured read that proves draft state and Windows prerequisites, validates both before credential access, and passes them with the expected tag to `scripts/upload-release-assets-by-id.sh`. Immediately before any delete or upload, the helper re-reads the immutable ID and requires the release still be a draft, still own the expected tag, and still report the captured upload URL; only then does it list collisions, delete an exact name by numeric asset ID, and post the replacement's raw bytes. Fake-GitHub control proves duplicate replacement, by-ID endpoints, ordered failure, tag rejection, and zero DELETE/POST calls when the release is public or has the wrong tag. Isolated restorations of the old tag upload and a bypassed fresh-draft guard each fail by name. A read-only v1.4.4 metadata check confirms the live numeric-ID/upload-URL shape and ten assets, but no upload was attempted. This local guard is uncommitted and does not alter the public release.

  - ✅ **Desktop release-verifier single-read resilience AUDIT CONFIRMED (Codex 2026-08-15 16:23 CDT; Claude 17:52 CDT).** The focused verifier previously reopened `scripts/release.sh` once for its exact-call guard and twelve more times for structural line discovery; because the release path invokes the verifier directly and again through `test:release`, one release run could perform 26 redundant source opens before adjacent guards. `scripts/test-desktop-release-verification.sh` now snapshots `release.sh` once per invocation and feeds the unchanged exact-match and line-order probes from that in-memory source. A fail-closed `grep` shim rejected the pre-fix direct file operand and now permits the complete focused verifier; current inspection finds zero `grep ... scripts/release.sh` operands, exactly one Bash source load, and source SHA-256 `8833d4e51046ea59db0870e2c6f046aa8bdb09bb0a3630bc1d8b4657dfc046aa`. Shell syntax and both systems' focused controls pass. Codex's isolated missing-publication mutation retains its exact refusal; Claude independently deleted the Gatekeeper line and received `release.sh must retain its Gatekeeper check`. The full `npm run --ignore-scripts test:release` body exits 0 in Codex's 17.7-second run and Claude's independent 18.0-second run. This closes the earlier pre-assertion full-chain measurement blocker without claiming that the broader host/vault incident is repaired; adjacent release tests still perform their own source reads. No release, build, signature, asset, credential, or store state changed.

- [ ] The required out-of-band theology database has a verified off-machine master matching `store/external-artifacts.md`; a Git commit alone does not contain it.
  - ✅ **External-artifact guard and effective-semantics follow-up AUDIT CONFIRMED (Claude 07:06 and 07:20 CDT; Codex exact replay 07:27 CDT).** Claude first confirmed identity, same-size checksum, rights, narrow deploy-exclusion, and synchronized false-backup protections, then independently ran a 7/7 battery covering basename/recursive/character-class exclusions, later Git negation, commented active Docker `COPY`, duplicate contradictory section, and ordered exact re-inclusion. Codex's fresh exact replay returns control/re-inclusion true and all six negative cases false. `scripts/theology-external-artifact.mjs` evaluates ordered ignore semantics for the exact database path, requires effective Git-ignore evidence plus untracked state, accepts only one active exact Docker copy, and requires unique exact manifest/checklist/ledger records and source ranges. Focused control is **21/21**. In complete wildcard fixture `/private/tmp/operator-theology-ignore.NPhJyQ`, appending only `**/*.sqlite3` adds only `Theology external artifact matches its canonical identity, rights, deploy, and unverified-backup records`, producing **173/1/5**; restored real-tree control remains **174/1/4**. The local database remains exactly 7,016,448 bytes with SHA-256 `3acfde0f0dae33963cfb1302773d670b521982e616c76254ac7788f9ffefc134`. This proves local identity and fail-closed record honesty only; it does not prove or create an off-machine backup, and this parent checkbox remains intentionally open.
- [ ] The desktop license signing key has a verified off-machine backup recorded in `store/external-artifacts.md`; existence on this Mac is not a backup.
  - ✅ **Value-safe desktop signing-backup record guard AUDIT CONFIRMED (Codex 07:26; Claude 07:37 CDT).** The guard validates only the manifest's expected home-relative paths, dated existence statement, purpose, explicit `UNVERIFIED` boundary, exact unchecked checklist state, blocked ledger state, and effective Git/Railway exclusions for hypothetical repository copies. It does not resolve, open, hash, print, copy, or locate either key or any backup. Focused coverage is **20/20**. Claude independently reproduced the full-tree control and six isolated record/path/ignore/duplicate/contradiction kills; Codex freshly reran the retained synchronized fixture at **173/1/5** with only `Desktop license-signing backup remains explicitly unverified and secret paths stay excluded` added. Restored real-tree control remains **174/1/4**, and this parent checkbox stays intentionally open.
- [ ] When the Android upload identity is created, its encrypted off-machine backup is verified and recorded before the first Play upload.
  - ✅ **Value-safe Android signing-record guard AUDIT CONFIRMED (Codex 07:45 and 08:30; Claude 08:27 CDT).** The guard binds the exact unconfigured manifest state, all three open Android signing/build checklist rows, both blocked ledger rows, effective Git ignore/untracked evidence, the four exact environment-variable names as presence-only booleans, known-file absence booleans, and one exact contiguous active Gradle all-four-input/fail-closed closure. It never reads or prints an environment value, properties file, private key, fingerprint, alias, password, or backup location. Claude independently confirmed the guard with focused **23/23** and five complete-fixture mutation classes. Codex then reproduced and closed an exact-line-preserving control-flow bypass that overwrote the computed release-request flag before the throw condition: the safe boolean probe changed from control/bypass `true/true` before the fix to `true/false` after it. In `/private/tmp/operator-android-guard-flow.2NN5vJ`, that isolated injection adds only `Android upload identity remains unconfigured, unbacked, and fail-closed without reading credential values`, producing **173/1/5**; restored real-tree control is **174/1/4**. This parent and both other Android signing/build parents stay intentionally open; the dated dry-run was not rerun.

- [ ] 🔴 **THE POST-BUILD-6 AUTHORITY/LINEAGE ORACLE IS CURRENT; THREE NAMED STATIC FAILURES REMAIN.** Apple and Google completion-packet reads prefer their canonical repository paths and fall back only to their exact filenames in the approved `System/AI-Collaboration/store-packets/` vault archive; the ignored action packet remains repository-local and cannot silently use that fallback. The focused retention suite passes **38/38**, the target-authority command passes **55/55**, and the direct readiness command completes at **179 passed · 1 warning · 3 failed**. The three failures are synchronized packet retention plus the Apple and Android screenshot holds. Build 6 is source-bound, archived, exported, and accepted for upload, but the retained receipt says only that the package is processing; processing completion/selectability, listing attachment, build-6-proven screenshots, physical smoke, sandbox purchase, and submission remain unproved. The packet route and post-build-6 oracle are locally corrected with Claude audit pending; the synchronized three-packet receipt remains absent. Static checks alone cannot establish submission readiness.
  - 🧾 **Build-6 archive/upload artifact receipt independently verified by Codex, 2026-08-16 15:12 CDT.** Archive identity is `com.base1520.theoperator` `1.4.2 (6)`; the archive log ends `** ARCHIVE SUCCEEDED **` with zero errors and five dependency/tooling warnings. The exported App Store IPA carries a distribution provisioning profile for team `6UP72M96Q5` with `get-task-allow = false` and `beta-reports-active = true`; its public tree exactly matches both the archive and current synced iOS payload. The retained Xcode upload logs report success with no errors and a processing receipt. Local strict codesign verification stops at `CSSMERR_TP_NOT_TRUSTED`, so no local trust-chain-validity claim is made; this does not override the accepted-upload receipt. Processing completion/selectability and listing selection remain unproved.
  - 🧪 **Android screenshot plan/ledger one-line bypass closed locally; Claude audit pending (Codex, 2026-08-16 16:38 CDT).** The readiness assertion previously trusted only the literal `> Android creative hold:` sentence in `store/screenshots.md`; removing that one line made the predicate locally clear even while the active ledger still marked both tablet Desk files `CREATIVE HOLD` and the audited gate `SCREENSHOTS STILL BLOCKED`. The shared provenance helper now treats either active ledger marker as an open Android screenshot gate, and the same named readiness assertion requires both absence of the plan hold and plan/ledger consistency. Focused control is **12/12**; the exact removal-only mutation with the canonical ledger unchanged is rejected by name. Direct readiness remains intentionally **179 passed · 1 warning · 3 failed**. Both staged Desk images still visibly show `SYNC PENDI...`, and the 10-inch image still shows the commentary-index error; neither screenshot hold is cleared and no capture, upload, console, or submission state changed.
  - 🧪 **Android positive screenshot-receipt guard closed and custody-hardened locally (Codex, 2026-08-17 06:36 CDT; independent final PASS).** `store/android-screenshot-verification.json` records the current `hold`, exact Android build identity `com.base1520.theoperator` / `1.4.2` / version code `1`, and all six current hashes without claiming a reviewed source. Clearance requires a `verified` visual `PASS` bound to immutable tagged build-6 candidate `729f98c38b058b355f9937b3be3def16cd5e86d7`, candidate-derived build identity, exact live bytes, and separately committed/pushed receipt custody. The current receipt is untracked and custody-invalid. Focused provenance passes **35/35** and direct readiness remains **179 passed · 1 warning · 3 failed**. This is not capture-from-package, pixel-quality, physical-runtime, or submission proof.
  - 🧪 **Apple/Android staged screenshot receipt universe closed locally; independent Codex audit PASS; Claude audit pending (Codex, 2026-08-17 00:44 CDT).** The readiness loop now retains the exact visible regular-file paths it already format-checks and supplies those dynamic Apple/Android sets to the visual receipt validators. The validators require exact receipt-to-disk path/hash equality under the canonical platform directories and reject omissions, receipt-only extras, duplicates, aliases, traversal/backslashes, and nested or non-canonical paths; visible symlink, directory, FIFO, or other non-regular entries fail the existing structural assertion before readiness. Focused coverage passes **27/27**; an independent read-only **26-assertion** matrix plus symlink probe returns PASS; target authority is **21/21**; and the full static board remains **179/1/3** on synchronized packet retention plus the unchanged Apple and Android screenshot holds. Current receipts and staged pixels are unchanged; this guard does not establish visual quality, candidate capture provenance, physical runtime, or submission readiness.
  - 🧪 **Screenshot receipt trust boundary is executable and accurately named; independent Codex audit PASS; Claude audit pending (Codex, 2026-08-17 00:50 CDT).** Synthetic Apple/Android receipts can satisfy the byte/source/build/declared-review contract while the canonical visual holds remain true; therefore receipt validity is not pixel-quality proof. The only two active `pixel-bound` test labels now use `source/build/byte-anchored visual-attestation`, and a new Android conjunction regression proves a valid synthetic receipt cannot override the separate pixel-quality hold. Focused coverage is **28/28**, all active labels were independently inventoried with zero misleading matches, target authority is **21/21**, and the full static board remains **179/1/3**. Production helper/readiness hashes, receipts, and screenshot bytes are unchanged; both visual parents stay red.
  - 🧪 **Root desktop package/lock parity closed locally; independent scoped review PASS; Claude audit pending (Codex, 2026-08-16 15:22 CDT).** Both root `package-lock.json` declarations now match `package.json` at `1.4.5`. `mobileVersionContract` adds a separately named desktop-lock assertion, the real focused test reads the actual package and lock bytes, and readiness passes both lock declarations into the shared contract. Syntax, exact real-file parity, and focused control pass at **11/11**. In `/private/tmp/operator-root-lock-parity.YUXBvT`, changing only the top-level lock version back to `1.4.4` exits 1 at **10/11**, failing exactly `root package and both lockfile declarations match`; independent synthetic top-level and root-entry drifts also fail the named contract. A separate read-only replay confirms control has no failure and top-level-only, root-only, and combined drift each yield exactly `Desktop lockfile version matches its package`. The wired `pretest:release` executes the new **11/11** control, then continues through screenshot **10/0** and live-store **9/0** before stopping on the separately recorded missing Apple-packet `ENOENT`. This closes the metadata-parity defect only. R1 still does not perform a lock-derived isolated install, the packet-backed aggregate remains non-runnable, and no iOS processing/selectability or submission state is promoted.
  - ✅ **Android creative-hold fail-closed guard AUDIT CONFIRMED (Codex 05:03; Claude 05:20 CDT).** `store/screenshots.md` explicitly says the staged 7-inch and 10-inch tablet Desk images must not be uploaded, but the readiness checker previously converted that exact hold into only a warning: the pre-fix complete board was **174/2/3**. `scripts/screenshot-provenance.mjs` now exposes the shared hold predicate, `scripts/check-mobile-store-readiness.mjs` fails on it by the named Android screenshot assertion, and the focused provenance suite is **10/0**. In disposable fixture `/private/tmp/operator-android-hold.cqtP0W`, restoring the warning-only call produces **9/1**, failing only `the mobile readiness gate fails on the shared Android creative hold`. Claude independently reproduced the focused control, exact mutation kill, and complete **174/1/4** board; Codex independently repeated the focused control and mutation after that verdict. This guard changes local accounting only; no screenshot, emulator, build, upload, console, or submission state changed.
  - 🧪 **Build-5 forward-action ledger guard / Claude audit pending (Codex, 2026-08-14 11:04 CDT).** The ledger's summary and candidate rows already said build 4 was superseded, but `External release gates` still instructed the operator to attach build 4 and take build-4 screenshots, while `Signing evidence still required` requested its listing-selection receipt. Before the fix, that exact stale state passed every non-screenshot assertion at **167/2/1**. Those two action rows now require build 5 and explicitly keep build 4 historical. `scripts/check-mobile-store-readiness.mjs` reads only the forward-action sections and requires the build-5 candidate/capture/receipt boundary while rejecting build-4 attachment or selection language. Restored control is **168/2/1**. In an isolated read-only fixture, the printed stale build-4 action at ledger line 141 fails exactly `Release ledger forward actions preserve the build-5 candidate boundary`, producing **167/2/2** beside the unchanged screenshot hold. This protects instructions; it does not create, upload, select, or validate build 5.
  - 🧪 **Historical build-4 checklist boundary / Claude audit pending (Codex, 2026-08-14 11:27 CDT).** The checked historical upload row still ended by telling the operator to wait for build 4 and explicitly switch the listing, contradicting the governing build-5 boundary. That stale sentence passed the pre-fix gate at **168/2/1**. `scripts/check-mobile-store-readiness.mjs` scopes the corrected processing/selectability action. The isolated printed original failed exactly `Release checklist keeps historical build 4 out of the final selection path` beside the screenshot hold. This proves the local action boundary only; its then-true statement that build 5 did not exist is superseded by the 14:20 upload.
  - ✅ **Prior current-tip/mobile-source wording guard AUDIT CONFIRMED mechanically by Claude, then superseded by build 5 (Codex 14:16; Claude 14:30 CDT).** Claude reproduced the named failure when the old branch-tip equality was restored. Twenty minutes later, pushed commit `8358c5f` changed the native Xcode project and invalidated the guard's phrase-pinned `ac04383` boundary. That older evidence remains valid for the exact historical mutation but is not current release-state proof.
  - ✅ **Uploaded-artifact/candidate distinction and active-banner identity guard AUDIT CONFIRMED (Codex 06:40; Claude 06:50 CDT).** The governing rows make builds 3–5 historical and build 6 the only executable path absent a new Cole decision. Fresh `HEAD` and `origin/main` remain `f5427c6730a78f055e3558fa5fda9e572d883740`, while build 5 remains bound to historical source `8358c5f3d3e929853f8a192b3d0c6ba3c92c5245`. Claude independently killed both the exact active-banner residual and its `1.4.2 (5)` alias by `current latest reconciliation does not label build 5 as the current candidate`, with restored focused **19/0**. Codex independently reran the retained complete fixture: the exact residual adds only `Release records preserve authority-parser audit confirmation without claiming submission readiness`, producing **173/1/5** beside the unchanged four blockers; the focused exact-array fixture predictably cascades at **3 passed · 16 failed** because the canonical violation accompanies other mutations, while real-tree control remains **19/0** and **174/1/4**. This audit promotes record integrity only; it does not prove build 6, processing, selection, screenshots, runtime, console state, or submission readiness.
  - ✅ **Build-5 artifact/current-source record parity guard AUDIT CONFIRMED by Claude at 17:16 CDT (implemented by Codex, 16:50 CDT).** The active ledger's native-package row still said the current gate was red “only” for screenshots even though the next-build PREACH source already made package age red. `scripts/check-mobile-store-readiness.mjs:356-366` now parses that exact row, requires build-5 byte parity to stay separate from newer working source, requires both present reds, and rejects screenshot-only wording. Corrected control is **171/2/2**. Codex's isolated printed contradiction failed exactly `Release ledger separates build-5 artifact parity from newer working source` at **170/2/3**; Claude independently restored the same screenshot-only wording in a valid full-tree fixture and reproduced the same named **170/2/3** result beside package age and the screenshot hold. This proves record integrity only; build 5 remains unchanged and does not contain the PREACH fix.
  - 🧪 **One-capture iPad completion-boundary guard / Claude audit pending (Codex, 2026-08-14 18:05 CDT).** The top gate had been reconciled to two clean build-5 landscape slots ready and one active-Preach original missing, but no assertion read that exact completion boundary: an isolated printed regression back to `three remaining iPad screenshots` stayed invisible at the pre-fix **171/2/2**. `scripts/check-mobile-store-readiness.mjs:368-380` now scopes the canonical top row, requires both the one-capture action and two-ready/one-missing evidence, and rejects the stale phrase. Restored control is **172/2/2**, with the same package-age and screenshot-hold failures. In `/private/tmp/operator-ipad-summary-pre2.remcB2/project`, the exact stale heading was printed and failed only `Release checklist preserves the one-capture iPad screenshot completion boundary`, yielding **171/2/3**; restoring the row returned **172/2/2**. This guards release instructions only and does not stage, inspect, upload, or approve a screenshot.
  - 🧪 **Next-candidate forward-action derivation guard / Claude audit pending (Codex, 2026-08-14 22:42 CDT).** The governing lineage row already proved archived build 5 predates current pushed source, but the checklist, ledger, screenshot plan, and Apple completion packet still directed final attachment, captures, or receipts around build 5, and the checker actively required some stale phrases. The forward-action checks now derive build 6 as archived build 5 plus one, consume every corrected source, require a provenance-clean build-6 package/processing/selection/screenshot boundary, and reject attach/select/submit or final-capture instructions for build 5. Shared control is **171/2/3** on the same honest lineage, package-age, and screenshot-hold blockers. Reverting the three core action rows fails both named properties at **169/2/5**; restoring only the stale screenshot-plan instruction fails the ledger-forward property at **170/2/4**. This changes instructions and proof only; no build, screenshot, console, or submission state changed.
  - ✅ **Submission-target authority reconciled; correction and alias/adversative follow-up AUDIT CONFIRMED by Claude at 02:05 CDT; post-build-6 active-Markdown record oracle locally updated.** No recorded Cole decision between the historical Path A / Path B options is being invented. Subsequent candidate evidence proves build 5 fails required C05; this is an evidence-derived gate consequence, not a recorded or retroactive Path B decision. Under the current fail-closed execution plan, build 5 must not be attached, selected, or submitted; build 6 is the only executable submission path after processing/selectability is proven and under fresh action-time approval, unless Cole makes a new explicit decision and the canonical guard/records are deliberately revised. Build 6 is now source-bound and accepted for upload, while processing completion, selectability, and listing attachment remain unproved. Historical Path A / Path B entries remain dated history, not current authorization. Claude's independent 7/7 battery covers the original adversative, alias, reverse-order, historical, conditional, and inline-code cases. The current shared selector excludes inactive Markdown and multiline/arbitrary-length inline-code evidence, requires the canonical structural prefix and one active semantic marker, requires each of the five direct authority records to remain one closed physical line, and shares the same semantic overclaim refusals between the focused oracle and direct checker across the current checklist and ledger summary/authority records; focused control is **55/55**, and the complete static gate remains intentionally red on its three recorded blockers. Claude audit of this direct-checker integration remains pending.
  - ✅ **Active latest-banner false-readiness scope and exact four-failure-board follow-up AUDIT CONFIRMED by Claude at 05:56 CDT.** Claude independently reproduced the exact and `1.4.2 (6)` false-readiness kills, balanced-inline-code control, and pre-Android-board kill. Before the follow-up, replacing the active banner with the superseded three-failure **174/2/3** board still passed the focused suite at **15/0**. The validator and shared checker now require the exact four-failure clause plus `The complete checker independently reproduces **174/1/4**.`. Claude's then-current isolated board regression produced the one intended canonical violation while predictably cascading through exact-array self-fixtures; Codex independently reproduces the same mechanism against the expanded suite at **1 passed · 16 failed**, and the restored current control is **17/0**. This is fail-closed fixture strictness, not parser collapse. The complete checker remains intentionally **174/1/4**. This protects record truth only; no build, upload, console, screenshot, or submission state changed.
  - 🧪 **Mutation proof — AUDIT CONFIRMED by Claude at 21:01 CDT:** the original predicate-bypass and readiness-call-site mutations each failed at **4/5**. After the partial iPhone recapture, replacing the new unchecked-gate detector with the obsolete `/Recapture all five against build 4/` phrase was printed at helper line 2 and failed the named `an open Apple screenshot gate without a declared hold is rejected` property at **4/5**, exit 1. Claude independently reproduced the **5/5** control, that same named **4/5** mutation failure, and the restored **5/5** result. These controls prove the predicate and its live gate wiring; they do not inspect screenshot pixels or establish submission readiness.
  - 🧪 Every Apple and Google transcription row is derived from the canonical entitlement/catalog data, including price, allowance, period, IDs, and the five-product iOS boundary. A `$29.99 → $30.00` mutation and removal of Heavy Annual's iOS prohibition each print the changed row and fail exactly this check at 156/2/1.
  - ✅ **Server-version parity audit-confirmed by Claude.** `server/package.json` and both root entries in `server/package-lock.json` match 1.4.2, and the static gate checks both server package/store parity and server lockfile/package parity. Baseline is 154/2/0. Codex's server-package mutation failed both assertions; Claude independently changed only the lockfile and got exactly the lockfile assertion failing at 153/2/1, proving the checks are independent. Production `/health` reports 1.4.2 and the **version assertion** passes. After the public privacy publication, a fresh Apple-scoped live gate is **40/0/0** (Codex and Claude, 2026-08-14 14:34 CDT).
  - 🧪 **Mutation proof:** reverting Android `versionName` to 1.4.1 was printed and failed the Android assertion. Claude also independently proved the Xcode parser catches a Release-only version, bundle-ID, or build-number mismatch while correctly ignoring Debug-only drift; removing its Release anchor fails all three checks closed.
- [x] `npm run test:release` passes — **fresh named script-body run AUDIT CONFIRMED (Codex 2026-08-15 16:23 CDT; Claude 17:52 CDT).** `npm run --ignore-scripts test:release` completed with exit 0 in 17.7 seconds for Codex and 18.0 seconds independently for Claude after the desktop verifier's single-read fix; both reached the script body's final `tsc --noEmit` step. `--ignore-scripts` deliberately excludes npm pre/post lifecycle hooks, so this is evidence for the exact named body rather than a normal lifecycle run. The independently reproduced **980 passed, 0 failed** count remains the audit-confirmed historical pre-`ac04383` baseline, not a current aggregate; no new aggregate is claimed because the suite mixes reporter formats. The focused 8/8 version-contract unit remains separately **AUDIT CONFIRMED (Claude, 2026-08-13 16:36 CDT)**. This source-regression pass does not establish mobile candidate freshness or store submission readiness.
  - 🧪 **Tablet-desk UI guard / logic audit-confirmed by Claude at 10:06 CDT; placement correction AUDIT CONFIRMED (Claude, 10:2x CDT — relocated guard 8/8 at `scripts/` path, `src/mobile` copy absent, wiring verified, fresh static gate 167/2/1 with sole screenshot red).** The pre-fix full release suite stayed green after a printed `commentary` hidden-state mutation changed the actual runtime seed from 7 visible / 6 hidden to 8 / 5. `scripts/test-tablet-desk-ui.mjs` now executes the actual model, AST-extracted actual flow/serialization adapters, and server-rendered actual SVG; it also checks the three intended dagger surfaces and add-tile copy. Restored control is **8/8**. Five isolated printed mutations fail: hidden seed **6/8**, serialized hidden state **7/8**, centering translate **7/8**, dagger glyph **7/8**, and `+ TILES` label **7/8**. Claude independently reproduced the 8/8 control and hidden-seed 6/8 result before a mechanical relocation. The first placement under `src/mobile/` made the package-age gate treat the test as shippable source and added a false second static failure (**166/2/2**); moving the unchanged guard to `scripts/`, rewiring `package.json:29`, and rerunning the relocated hidden mutation restores **8/8**, **6/8**, and the honest **167/2/1** static gate. This proves those source guarantees; it does not prove App Store Connect packaging or touch interaction.
  - ✅ **Silent PREACH-gate affordance AUDIT CONFIRMED by Claude at 16:51 CDT; subsequently packaged in build 6 (implemented by Codex, 16:31 CDT).** `src/mobile/TabletSermonDesk.tsx:571-581,621-622,632` keeps the 40-character manuscript-body gate but makes PREACH tappable: an unready tap renders the existing alert surface with the exact requirement and does not open Preach Mode; a ready tap opens it and clears stale notice state. `scripts/test-tablet-desk-ui.mjs:118-255` executes the actual component through a persistent hook harness and passes **10/10**. Claude independently confirmed that the test invokes the real button handler, does not duplicate the readiness predicate, and that restoring the old disabled line fails the named not-ready behavior at **9/10** while the ready path remains green; Codex freshly reproduced both results after receiving the verdict. `npm run pretest:release` and `npx tsc --noEmit` exit 0. Build 5 and its uploaded/installed bytes do not contain the fix; source-bound build 6 does. A build-5 Preach-Mode screenshot remains valid only as a historical visual draft because the fix changes only the not-ready button behavior; final use requires pixel-equivalence proof against packaged build 6. At the 16:51 audit, the static state was **172/2/2** on package age plus the screenshot hold; the current complete result is recorded in the top gate row. No readiness checkbox is promoted.
  - 📈 **685 → 717 (+32)** — the guards added overnight: access-code policy, release provenance, Windows release workflow, plus billing-period and readiness additions. The prior 685 figure was measured 2026-08-12 06:2x, **before** the iOS platform filter, the `.99` repricing, and every source change of the night. This is the first full-gate run against the source that build 3 was packaged from.
- [x] `npm run mobile:sync` completes from a clean build output.
  - ✅ **2026-08-13 build-4 reconciliation:** the provenance guard first refused uncommitted and committed-but-unpushed inputs, then the clean pushed-source sync completed. The readiness gate and archive comparison confirm the full-release subscription UI and catalog guard are present in the current iOS payload; Android/iOS packaged-copy parity remains covered by the gate.
  - ⚠️ **2026-08-14 post-upload sync boundary:** Claude ran the guard against pushed `ac04383` and regenerated/installed current iOS bytes after temporarily preserving Codex's `package.json` diff. That makes the local packaged tree source-current, but it cannot retroactively change Apple's already-uploaded build 4 and is not a build-5 archive/upload receipt.
- [x] Synced iOS and Android assets were generated with `VITE_OPERATOR_RELEASE_STAGE=full` and `VITE_ESV_MOBILE_LICENSED=false`.
- [x] Packaged native assets contain no simulator fallback, mock/test purchase, or development-checkout copy.
- [x] New iPhone/iPad subscriptions invoke native StoreKit from the app, bind the receipt to the verified Operator account, and never redirect to website checkout.
- [x] iPhone and iPad simulator builds pass with Xcode 26 or later.
  - ✅ **Re-verified by Claude 2026-08-13 09:4x against build-3 source.** `npm run mobile:verify:ios` → **exit 0, zero `error:` lines**; only third-party warnings from `node_modules/@capacitor/app/…/AppPlugin.swift` and the standard `[CP] Embed Pods Frameworks` phase notice. (No `** BUILD SUCCEEDED **` banner appears because the script runs `xcodebuild -quiet`; the exit status is the verdict.) Run with `CODE_SIGNING_ALLOWED=NO` — a pure compile check that cannot touch signing or provisioning assets.
  - **Why it needed re-running:** this row was previously checked and **undated**, and every material input changed after it was last true — the iOS platform filter rewrote `src/mobile/store.ts`, prices moved to `.99`, `mobile:sync` rebuilt the bundle, and build 3 was packaged. **`tsc --noEmit` passing in the release gate does not prove the native Xcode build compiles** — different toolchain, different failure modes. Surfaced by the undated-claim sweep, not by hand.
- [x] Android debug compilation passes with target API 36.
  - ✅ **Re-verified by Claude 2026-08-13 10:0x against build-3 source.** `npm run mobile:verify:android` (`clean assembleDebug`) → **`BUILD SUCCESSFUL in 8m 27s`, exit 0, zero error-shaped lines.**
  - **Why it needed re-running:** the iOS Heavy-Annual exclusion rewrote `src/mobile/store.ts`, which is **shared** — the same file serves both platforms, dropping a plan on iOS while Android resolves all six on a composite `googleProductId` + `androidBasePlanId` key. Reading that logic is not compiling it, and shared code is exactly where one platform breaks while the other looks fine.
  - 📌 **Note for future auditors: Gradle prints `BUILD SUCCESSFUL`; the iOS script does not.** `mobile:verify:ios` runs `xcodebuild -quiet`, which suppresses the `** BUILD SUCCEEDED **` banner — searching for it there finds nothing and can read as failure. **On iOS the exit status is the only verdict.**
- [ ] A signed Android release bundle compiles with the authorized upload key.
  - 🔴 **Fresh runtime blocker, Codex 2026-08-13 21:46 CDT.** All four `OPERATOR_UPLOAD_*` inputs are absent from the current release shell, and neither `android/operator-upload.properties` nor the example's default `android/operator-upload.jks` exists. With the Java/Android SDK/cache environment pinned by the `mobile:verify:android` package script, `./gradlew :app:bundleRelease --dry-run --offline` reached the fail-closed guard in `android/app/build.gradle` and failed exactly `Operator release signing is not configured`, before any bundle task executed. This proves the current release environment cannot sign; it does not prove an older key cannot exist in an unconfigured external backup.
  - 🔴 **Value-free absence rechecked, Codex 2026-08-15 22:58 CDT; the older exact guard proof above remains authoritative.** The current shell again exposes zero `OPERATOR_UPLOAD_*` input names; `android/operator-upload.properties` and `android/operator-upload.jks` remain absent; and a recursive Android-tree scan finds no `.jks`, `.keystore`, or `.aab` artifact. Today's installed-wrapper `:app:bundleRelease --dry-run --offline --no-daemon` stopped before project configuration because the pinned Android Gradle and Google Services plugins were not cached; the corresponding online dry run remained in dependency resolution for a bounded 120 seconds and was stopped before any task executed. Therefore today's replay does **not** independently reproduce the exact signing-guard error, but it does reproduce the current input/artifact absence and a precise local build-replay blocker. No key, credential, signed bundle, console state, or deployment was created.
- [ ] Physical iPhone/iPad and Android smoke tests cover registration, study, library, account, deletion, microphone, export, and offline saved-study access.
  - 🧭 **Execution packet added; no runtime box promoted (Codex, 2026-08-15 00:35 CDT).** `store/mobile-physical-smoke-matrix.md` now binds each run to one exact TestFlight/Play-internal candidate and provides result/evidence slots for install identity, real-inbox registration, study, library persistence, account/device access, offline saved-study access, microphone/recording, manuscript export, authorized sandbox/test purchase and restore, deletion on a disposable non-review account, and a surface-by-surface visible-control traversal. It forbids secrets and reviewer-account deletion in evidence. Creating or statically inspecting the packet proves none of those behaviors; this parent remains open until every applicable row is physically executed on the exact distributed candidate.
  - ✅ **Packet contract AUDIT CONFIRMED and persistently fail-closed; runtime remains open (Codex 00:51; Claude 01:06 CDT).** `scripts/test-mobile-physical-smoke-matrix.mjs` runs in both `npm run pretest:release` and the README-mandated `npm run mobile:store:check`. It requires the exact four-device candidate identity, all M01–M11 functional rows, all C01–C06 per-device control traversals, actionable row content, disposable-account/reviewer-account safety, purchase/restore through the authorized store channel, the build-5 PREACH known-positive, and the explicit static-versus-runtime boundary in both packet and README. Its control and eight targeted mutations pass **9/0**. Claude independently reproduced the wired pretest and five label-preserving mutants; Codex separately removed one C05 per-device result cell while retaining its label and reproduced exit 1 at exact `visible-control rows retain traversal and valid per-device result cells`. The complete store check remains honestly **171 passed · 2 warnings · 3 failed** on current-source/build lineage, packaged-bundle age, and the Apple screenshot hold. Neither parent box is promoted until the exact distributed candidate is physically executed and evidenced.
  - ✅ **Functional per-device evidence contract AUDIT CONFIRMED (Codex 03:48; Claude 04:05 CDT).** Before this correction, M01–M11 had one shared `Result` cell even when a lane named all four device classes; a disposable fixture marked only M02 `PASS` with `iPad-only observation`, and the focused suite still passed **12/0**. The packet now provides separate iPhone, iPad, Android-phone, and Android-tablet result cells for every functional lane, marks the phone cells of tablet-only M07/M08 explicitly `N/A — tablet only`, and says a pass on one device is not evidence for another. Claude independently collapsed M02 to the old shared result and blanked M07's phone N/A markers; each failed the exact named contract, and the restored current aggregate is **17/0**. This confirms only the execution artifact: every physical functional result remains open, no device was exercised, and the parent row remains unchecked.
  - ✅ **Recorded-result evidence requirement AUDIT CONFIRMED (Codex 04:04; Claude 04:20 CDT).** After the per-device fix, a disposable fixture could still set M02's iPad cell to `PASS` while leaving its evidence cell blank, and the real focused suite remained green at **14/0**. The packet now requires every `PASS`, `FAIL`, or `BLOCKED` result in the functional, legal-link, and visible-control matrices to carry a non-secret evidence pointer or precise blocker in that row's final cell. Claude independently exercised evidence-blank functional, control, and legal rows; all failed the exact named evidence contract in the restored **22/0** battery. This confirms only the artifact/guard: all runtime cells remain open and the parent row stays unchecked.
  - ✅ **Device-matched evidence binding AUDIT CONFIRMED (Codex 04:16; Claude 04:20 CDT).** The nonblank-evidence guard still allowed M03 to show four device `PASS` cells while its final cell contained only `iPad: iPad-only observation`; the real focused suite remained green at **17/0**. The packet now requires an exact non-secret `iPhone:`, `iPad:`, `Android phone:`, or `Android tablet:` evidence label for every device with a `PASS`, `FAIL`, or `BLOCKED` result, rejects duplicate/unknown labels, and rejects evidence labels for open or `N/A` devices. Claude independently killed the requested partial-evidence M03, M07, L01, and C01 cases by the exact named contract and restored **22/0**. No physical result, candidate readiness, or store state is promoted.
  - ✅ **Filled-packet fixture collision AUDIT CONFIRMED with all row classes covered (Codex 04:47; Claude 04:51 CDT).** Claude correctly found that two M03 self-mutations anchored on a pristine all-open row and therefore errored `mutation did not change input` after a valid fully labeled four-device result was recorded. Codex independently reproduced the exact defect in `/private/tmp/operator-filled-packet.DerCYr`: the canonical filled row itself validated, but the focused suite fell to **20/2**. The mutation battery now derives a deterministic open-results projection while continuing to validate the live packet unchanged. Persistent regressions cover all-device M03, tablet-only M07, legal-link L01, and control C01 simultaneously. Claude independently filled M03/L02/C02 in a complete-store fixture and measured **24/0**, then removed one required M03 label and saw only the first canonical validation fail by the named device-evidence contract at **23/1**, with no fixture errors; Codex independently reproduced that exact two-leg result in `/private/tmp/operator-claude-audit.T6mM0z`. The current open packet and Codex's separate fully evidenced four-class packet also pass **24/0**; partial C01 evidence produces the same exact canonical-only **23/1** kill while every self-fixture executes. Fully wired pretest exits 0. This confirms test infrastructure only; every physical result remains open.
  - 🧪 **Candidate-identity/result binding guard LOCAL; Claude audit pending (Codex, 2026-08-15 17:24 CDT).** The pre-edit verifier required the seven candidate-identity row labels but did not inspect their per-device values, so a device-specific `PASS`, `FAIL`, or `BLOCKED` could carry matching evidence while the tested candidate identity remained blank. The verifier now parses the exact seven-row table and, only for each device with a recorded result in M/L/C, requires all seven values, the platform-authorized `TestFlight` or `Play internal` channel, and canonical bundle/package ID `com.base1520.theoperator`. The canonical all-open packet remains valid. Persistent iPad probes reject blank identity and a `sideloaded` channel while accepting the same evidenced result after a complete TestFlight identity; the named suite is **27/0**. An independent read-only novel Android probe also kills wrong package ID, incomplete identity, and wrong channel for `PASS`, `FAIL`, and `BLOCKED`, while ignoring an unrecorded device. This is static guard evidence only: every physical result remains open, the parent stays unchecked, and the complete board remains **174/1/4**.
  - 🔴 **Phone device-link cap cause narrowed at runtime; privacy-safe diagnostic audit confirmed and later carried by a dirty CLI deployment; decisive replay still absent (Codex reconciliation 2026-08-16).** Value-filtered Railway HTTP metadata records three pre-diagnostic production `POST /v1/device-links/redeem` responses at **22:08:40Z, 22:09:17Z, and 22:09:33Z**, all **409**; no IP, user agent, request ID, header, body, account, device, or install value was read or recorded. Source tracing proves the desktop only creates the temporary code and that `DEVICE_LIMIT` is unique to this redeem path, so the proposed alternate desktop-route bypass is ruled out. Those earlier events cannot distinguish a fresh/different or legacy-null install identity from an account that still had at least three rows after replacement; uncapped token-issuance paths and the absence of `(account_id, install_id)` uniqueness keep the latter possible. Current source at `server/src/mobile.js:151-198` prepares one refusal-only marker containing only same-install match yes/no and the active count after the tentative revoke, emits it after rollback and client release, and cannot let a logging failure replace `DEVICE_LIMIT`. Focused control is **44/0**; the printed mutation disconnecting the marker from the UPDATE row count fails the named same-install assertion at **43/1**. This diagnostic is **AUDIT CONFIRMED (Claude, 2026-08-15 00:06 CDT)**. It remains uncommitted, but it is not undeployed: Claude's contemporaneous 10:55 execution record says successful CLI deployment `0973e6a3` carried the audited migrate and device-link units, and the 21:5x ship receipt says current successful CLI deployment `f9702bb2` came from the reconciled tree. Fresh Railway metadata corroborates both deployments; neither has a reproducible Git-source receipt. Server-filtered queries return no capacity marker on either deployment, so the telemetry branch has not been observed after deployment and the identity cause remains unresolved. One authorized phone relink replay reading only the two safe scalars remains necessary; the physical-smoke parent stays open.
  - ✅ **iPad build identity and launch proven 2026-08-13 08:36 CDT.** Xcode brought the connected `iPad (4)` online, installed the development-signed build-3 archive in place, launched `com.base1520.theoperator`, and read back `Version 1.4.2` / `Bundle Version 3`. This closes install/startup identity only; the functional smoke matrix and fresh screenshots remain open.
  - 🧪 **Fresh read-only identity, 2026-08-14 09:31 CDT:** the connected physical iPad reports installed app `The Operator`, bundle `com.base1520.theoperator`, version `1.4.2`, bundle version `4`. Because both the post-`ac04383` sideload and the historic App Store Connect upload use that same version/build identity, metadata alone cannot establish which bytes are installed. No functional smoke action or screenshot capture was performed by Codex in this unit.
  - 🟢 **Fresh value-free availability check, 2026-08-14 15:14 CDT:** CoreDevice reports exactly one physical iPad; it is paired, tunnel-connected, booted, and has developer-disk-image services available. Connectivity is therefore not the current screenshot blocker. This read-only check did not identify the device, inspect its contents, install or launch the app, or capture a screen; build-5 installation/provenance and the physical smoke matrix remain open.
  - 🟢 **Build-5 installed identity independently confirmed, 2026-08-14 15:29 CDT:** after Claude reported installing and launching build 5, Codex made a fresh read-only installed-app query. Exactly one app matched canonical bundle `com.base1520.theoperator`; it reports `The Operator` version `1.4.2`, bundle version `5`. This closes the device's candidate version/build identity prerequisite only. Codex did not install, launch, capture, inspect app content, or run the functional smoke matrix, and the query does not prove App Store processing/selectability or screenshot quality.
  - ⚪ **Prior value-free availability recheck, 2026-08-17 01:05 CDT:** the explicit Xcode CoreDevice inventory was filtered to iPad product types before output and hid every default column except state; it returned exactly one value, **`unavailable`**. This is retained as dated evidence and is superseded by the 12:53 CDT recheck below. Pairing state and installed Operator identity were not re-read, so the prior paired/build-5 facts remain dated evidence rather than current proof. The read-only inventory did not inspect app contents, launch or install anything, identify a cause, or expose a name, identifier, serial, network address, account value, or credential; cable, power, trust, lock, sleep, and host-service causes remain unproved.
  - 🟢 **Fresh identifier-free availability recheck, 2026-08-17 12:53 CDT:** the explicit Xcode CoreDevice inventory applied an iPad-name predicate before disclosure, hid every default column, and printed only `State`; it exited `0` with exactly one value, **`connected`**. This supersedes the 01:05 `unavailable` fact and removes device availability itself as the current Apple inspection/capture blocker. It does not establish current pairing/trust/unlock state, installed Operator identity, build-6/TestFlight provenance, app behavior, screenshot suitability, purchase behavior, or any store-console state. No name, identifier, serial, address, account value, credential, app content, install, launch, or capture was read or changed.
  - 🟢 **Fresh build-6 installed identity independently matched, 2026-08-17 13:12 CDT:** a read-only CoreDevice app inventory was restricted to canonical bundle `com.base1520.theoperator` before output and returned exactly one match: `The Operator`, version `1.4.2`, bundle version `6`. An independent reviewer repeated the same narrow query and matched the exact tuple. This supersedes build 5 as the current installed metadata and closes only the build-6 version/build identity prerequisite. The result cannot distinguish the development-signed build-6 archive from a TestFlight-distributed copy because both share this metadata; it does not prove channel or byte provenance, processing/selectability, launch or behavior, screenshot suitability, purchase behavior, console state, or submission readiness. No app was installed or launched, no app or private device content was inspected, and nothing on the device was changed.
  - 🟡 **CoreDevice developer-app classification independently matched, 2026-08-17 13:40 CDT:** the exact bundle-filtered inventory explicitly used `--no-include-default-apps` and still returned the one `The Operator | com.base1520.theoperator | 1.4.2 | 6` row. Local Xcode help defines a default app as one that is not a developer app and says this view includes only developer apps; an independent exact replay matched the row and semantics. This establishes only CoreDevice's current **developer-app classification**. It aligns with the reported development-signed capture session but does not prove exact installed bytes, signing or install provenance, a specific distribution channel, or TestFlight absence, and it cannot satisfy the physical packet's required `TestFlight` channel receipt.
  - 🧪 **Physical-evidence sensitive-data guard LOCAL; Claude audit pending (Codex, 2026-08-15 17:46 CDT).** The packet already forbade email addresses, verification/device-link/comp codes, bearers, receipt bodies, account/install IDs, and recording content, but the pre-edit verifier checked only that this warning text existed. An in-memory fixture with complete iPad identity, M02 `PASS`, and matching `iPad: cole@example.com` evidence returned no failure. The verifier now scans every M/L/C evidence cell and rejects email syntax, standalone six-digit values, `OPR-…` codes, labeled or HTTP-style bearer values, labeled verification/device-link/comp codes, receipt bodies, account/install IDs, and recording content. The persistent battery is **39/0**, including eleven prohibited forms plus safe local-pointer and value-free-blocker controls. An independent read-only cross-lane run is **43/0**: labeled and HTTP-style bearer values fail by the one exact sensitive-data property, a value-free bearer blocker passes, and a control-row device-link value fails. This is a pattern guard, not proof that a pointed-to artifact or arbitrary prose is secret-free; human redaction remains mandatory. Every runtime cell and parent checkbox remains open, and readiness remains **174/1/4**.
  - 🧪 **Candidate-consistency/no-mixed-builds guard LOCAL; Claude audit pending (Codex, 2026-08-15 18:01 CDT).** The packet already required one exact candidate and restart-on-source/build change, but the verifier compared no candidate values across recorded devices. A complete four-device M03 `PASS` fixture with four different version/build values and four different reviewed source commits therefore returned no failure at the restored pre-edit **39/0** control. The packet now requires `<marketing version> (<platform build number>)`; the verifier requires one marketing version and reviewed-source-commit string across every recorded device, exact version/build equality within the Apple pair and within the Android pair, and intentionally permits Apple and Android build numbers to differ. Persistent control and mutations pass **46/0**. Independent read-only review passes **5/0** novel probes: Apple-pair, Android-pair, cross-platform marketing-version, and source-commit mismatches each fail by their exact named property, while Apple build `101` and Android build `7` with one marketing version/commit pass. This static guard compares declared strings only; it does not verify Git object existence, candidate bytes, receipt/identity pointers, store-catalog stability, backend release stage, physical behavior, or submission readiness. Every runtime cell and the parent remain open; the board remains **174/1/4**.
  - ✅ **Candidate/result/evidence redaction false-pass closed; Codex independent adversarial re-audit PASS (2026-08-17 00:01 CDT).** The prior **46/0** verifier accepted candidate receipt value `receipt_body=private-receipt` alongside a complete iPad M02 `PASS`. The current guard scans candidate values, evidence, result reasons, and `N/A` reasons; it normalizes documented secret forms including Unicode, server-equivalent device links, issued `opr_` bearers, purchase/comp codes, Markdown/GFM/HTML concealment, and stable nested percent/entity encodings. All four exact rendered tables are unique, header-bound, and contiguous, so inactive or detached rows do not count. Every recorded device also requires the exact post-evidence attestation `PASS — compared privately; no configured code recorded`, because arbitrary configured codes cannot be proven safe lexically. Reviewed-source commit validation remains syntax-only until independent repository resolution. The focused suite passes **140/0**, and the frozen-hash independent adversarial audit passes. This is static guard evidence only: all runtime rows and the parent remain open, and the board remains **179/1/3**.
- [x] Public-domain translations load; ESV is absent.
- [x] AI-processing permission is required before generated requests and can be withdrawn.
- [x] Account creation requires possession of the six-digit code sent to the submitted email address.
- [x] Local edits are protected from remote-refresh overwrite, and linked anonymous studies/recordings transfer to the verified owner.
- [x] Revoked bearers lose the caller-supplied install identity, cannot read claimed studies, and never delete the account owner's local library.
- [x] Known and unknown recovery emails use the same cooldown ledger; registration metadata is account-bound and deleted with the account.
- [x] Workspace conflicts expose `USE CLOUD` and `KEEP THIS TABLET`; note and desk limits fail visibly; archived studies can be restored.
  - ✅ **Device-neutral source fix is audit-confirmed.** Shared tablet UI now uses `tablet` / `stylus`; Claude mutation-tested the static guard and independently confirmed the rebuilt source state. This does not clear the separate screenshot recapture hold.
- [ ] No staged, mock, disabled, or dead control is visible in the full store build.
  - 🔴 **Build 5 contains a known-positive failure; future-candidate physical traversal specified (Codex, 2026-08-15 00:35 CDT).** The preserved archive identifies `com.base1520.theoperator` `1.4.2 (5)`. Its `TabletSermonDesk-pVMOfsZQ.js` and both staged native copies are byte-identical (SHA-256 `7926f94f553af2a2e53a9d843b69c9040fcb856d5cbe123048ee74fdc0c332cc`) and render visible `PREACH →` with `disabled:!de`; `src/mobile/mobile.css` keeps disabled command-bar controls visible at opacity `.38`. Current reviewed source removes the disabled prop and makes a not-ready tap explain the exact 40-character manuscript-body prerequisite; the actual-component control is **10/0**. `store/mobile-physical-smoke-matrix.md` requires this known-positive regression plus every other visible control to be exercised on each applicable physical device. Static source/bundle inspection cannot clear this row, and build 5 must fail it.
- [ ] Final screenshot set contains no loading indicator, `SYNC PENDING`, clipped status text, or provider error.
- [x] Release source is committed and reproducible — **this is a PREREQUISITE of archiving, not a follow-up to it.**
  - **Mobile-source lineage, verified through build 6 and reconciled 2026-08-17:** canonical release source for uploaded build 6 is `729f98c38b058b355f9937b3be3def16cd5e86d7`; the uploaded artifact build number is `6`. Repository evidence may advance on clean pushed descendants of that immutable candidate only when all provenance-guarded mobile release inputs and synced native payloads remain byte-identical to the candidate; receipt evidence must never redefine the candidate source. The pushed evidence tip is `4b25c05db5054da079202a4ab05daf1048ee5502`; it is the last recorded pushed descendant whose provenance-guarded mobile release inputs and synced native payloads remain byte-identical to the candidate. Later pushed repository commits are permitted only by the exact pinned package-manifest transition while every other provenance-guarded input remains byte-identical to the evidence tip; such commits do not redefine the immutable candidate or evidence tip. The executable comparison covers the broad tracked iOS/Android source trees, a clean index/worktree against the current pushed repository head, exact candidate/evidence and current package-manifest SHA-256 pins, a package-lock normalization that permits only its two root version metadata fields, and a pinned 23-file runtime digest `68fd3aed717cbad04fccf4e52e9d260d34523687c4b4a289a267c5cc3b0d5a57` derived directly from the preserved build-6 IPA and required across `dist-mobile` plus both ignored native copies. Build 6's archive and exported IPA identify `com.base1520.theoperator` `1.4.2 (6)`, and the archive payload exactly matches the clean committed iOS public payload from that source. The upload receipt records success with no errors and says `Uploaded package is processing`; processing completion, selectability, and listing attachment remain unproven. No later native candidate is required for source parity, but build 6 must not be selected or submitted without the separate live-console and approval evidence.
  - **Historical build 4:** its build-number bump landed in `e93f376`, regenerated native payload in `550d708`, and both were pushed before archive/upload. It remains reproducible upload evidence but is superseded as a final candidate by the later app-source change.
  - **Historical build 3:** its defining source landed together in `3f692cf` and was pushed before archiving. It remains reproducible but is superseded because it predates the storefront guard.
  - ⚠️ **The wording of this row previously read "…after Cole approves the release commit,"** which implies committing happens *later*. For a mobile archive that ordering is backwards and is exactly how the defect recurs. **Commit → archive → upload.**
  - ✅ **The mobile path now enforces this.** `npm run mobile:sync` invokes `scripts/check-mobile-release-provenance.sh` before building or copying native assets. Its isolated mutation test proves a dirty release input is refused rather than silently packaged.
  - ✅ **Historical dirty-source packaging refusal + provenance-harness repair AUDIT CONFIRMED by Claude at 18:03 CDT (Codex, 2026-08-14 17:15 CDT); source-commit blocker superseded at 18:28 CDT; current four-input preflight blocker corrected at 03:38 CDT.** Before `ad46a16`, a real `npm run mobile:sync` exited 1 before Vite or Capacitor because `src/mobile/TabletSermonDesk.tsx` differed from `HEAD`; the uploaded/installed build-5 bundles remained untouched. The PREACH source and audited harness then landed in `ad46a16`/`f5427c6`, so an uncommitted PREACH file is no longer the current blocker. A fresh `bash scripts/check-mobile-release-provenance.sh` run at 03:34 CDT exits 1 before Vite/Capacitor on exactly four dirty release inputs: `M package.json`, `M src/App.tsx`, `M src/components/ScholarChat.tsx`, and `M src/lib/apiErrors.ts`; the former claim that `package.json` was the sole dirty input is superseded. `HEAD` and `origin/main` both resolve to `f5427c6730a78f055e3558fa5fda9e572d883740`. Independent review finds the package diff changes test-script wiring only—not dependencies, versions, or build settings—while the three source files form the hosted-reader restore/context/error-truth delta: restore may reuse verified local bytes or a remembered hosted study without minting fresh work, Scholar visibly distinguishes grounded from general answers, and the unfinished-reading refusal receives actionable copy. `package.json` parses; `git diff --check` and TypeScript pass; the matching restore and hosted-client behavioral controls pass **25/0** and **52/0**. The immediate packaging-preflight blocker is therefore a fresh-approved commit of the reviewed four-file release-input set; candidate lineage remains a separate store failure because build 5 predates pushed source, and no build 6 sync/package/archive/upload has been produced. The dedicated mutation harness provisions a disposable bare `origin`, pushes the baseline with upstream tracking, and passes while printing and killing release-input, transitive-import, mobile-entrypoint, and lockfile mutations. Claude independently reran the focused control, then applied the printed `push -qu` → `push -q` mutation in a valid full-tree fixture and reproduced exit 1 at the exact named no-upstream boundary. No stage, commit, sync, build, archive, upload, or submission occurred in this correction.
  - 🔴 **Repository-wide provenance blocker verified 2026-08-16 06:27 CDT; the four scoped mobile inputs are only the first refusal.** The scoped mobile check exits before it delegates to `scripts/check-release-provenance.sh`. Running that delegated guard directly exits 1 with `Working tree is not clean`. A fresh value-safe Git inventory contains exactly **64** entries: **39 modified tracked files + 25 untracked files + 0 staged entries**, with no deletions, renames, or binary tracked diffs. By top-level area: root **2**; `.claude` **1**; `.github` **1**; `electron` **5**; `scripts` **23**; `server` **20**; `src` **3**; `store` **8**; `website` **1**. The tracked delta is 2,860 insertions / 882 deletions across 39 text files. S1 community/generation extraction is source-reviewed with exact seven-route parity and failing mount/documentation mutations. H1/S2 continuity/no-double-charge behavior and only the corresponding H0/S0 hunks are source-reviewed after audit found and closed a completed-document recovery defect: an owned `done` document outside the verifier-only shared cache is returned under the same study id before any mint boundary, with account/anonymous ownership and lookup uncertainty failing closed. Desktop restore is **25/0**, H1 hosted transport is **52/0**, ridealong/registered-route behavior is **99/0**, meter is **80/0**, routes are **21/0**, and its three named mutations exit nonzero. H2/S3 context disclosure and standing-chat behavior plus only their H0/S0 hunks are now source-reviewed after this unit closed false unfinished-study grounding, stale same-reference disclosure, shared-IPC response breakage, duplicate metered-send exposure, conversation reset on mode change, false verified provenance, and contradictory no-packet role instructions. The focused hosted control is **67/0**, sermon-assist is **14/0**, owned-study access is **9/0**, the registered specialist route behaviorally rejects an anonymous full-stage caller before spend and distinguishes free from active-paid recurring Ask access, a 12-case four-agent prompt probe passes, and the full release body exits 0. Independent read-only source review returns no material residual; an independent 16-mutant matrix kills every named truth/access regression and the review manifest records the exact counts. H3's static field contract is source-reviewed at **20/0** with desktop resume **25/0** and independent mutation re-audit PASS; its source-bound physical execution remains **NOT RUN** and separate. These files remain uncommitted and unproved in live PostgreSQL/provider behavior, mounted Electron/React, a signed/package artifact, an uploaded release build, or physical legibility. The clean-worktree provenance control passes and full `git diff --check` is clean. A fresh-approved commit of the first four files is therefore necessary but not sufficient for `mobile:sync`: every remaining path still needs a reviewed disposition in coherent change sets, and the global guard must be rerun clean. This inventory does not authorize bundling unrelated work, staging, commit, deletion, revert, or index-policy changes.
- [ ] Non-secret console completion/action packets are retained in a reproducible approved location.
  - 🧪 **Approved relocation input routing restored locally; retention remains red (Codex 2026-08-16 15:45 CDT; independent read-only defect audit complete; Claude audit pending).** A shared exact-path loader checks the canonical repository path first, then permits only the relocated Apple and Google completion filenames under the approved vault archive; the ignored action packet has no vault fallback. Missing or unreadable inputs become named assertions rather than uncaught filesystem errors. The routing-era focused suite passed **25/25**; the expanded retention suite now passes **38/38**, all three current inputs resolve, and the post-build-6 target-authority control passes **55/55**. After the separate authority/lineage reconciliation, the direct checker completes at **179/1/3** and this retention assertion is one of those three honest failures. No synchronized receipt exists for all three exact current byte digests, so this parent remains unchecked. No packet was copied, moved, published, deleted, or exposed in either correction.
  - 🔴 **Current split retention inventory, 2026-08-17 11:36 CDT; supersedes the 2026-08-16 16:23 inventory.** All three operational working mirrors are present under `sermon-tool/store/`, are effectively ignored, and are absent from public-repository `HEAD 7875b3a99e774b1d98b0c10aaadde0f19daad052`. The Apple and Google mirrors are byte-identical to their tracked, clean approved Claude-vault authorities: Apple is 8,228 bytes at SHA-256 `774587ed002044ede23975c3058d43c233a1e1007386bd76eae46ca24764afeb`; Google is 12,625 bytes at SHA-256 `3f0bc8e2d164b2f16129aac21cd0b80853b8a00fea20acb61251e55c16eef7c8`. The 12,250-byte action packet remains only at ignored, untracked `store/console-action-packet.md`, at SHA-256 `b4a56abc383f12d92dc0412e43f4702b0fbeb5be7baf89cdc42f1fa4d3b2b20d`, and intentionally has no vault fallback or approved retention destination. The governing M5 disposition forbids automatically committing the ignored action packet. Therefore the exact blocker is fresh Cole approval for a reproducible private retention destination for the action packet, followed by one same-event synchronized three-packet receipt. This is a retention-policy gap, not a console-completion gap. The parent remains unchecked; no packet was copied, moved, deleted, published, or represented as retained by this inventory.
  - 🧪 **Synchronized retention-receipt gate implemented locally, Codex 2026-08-15 23:19 CDT; Claude audit pending.** A Git/`HEAD`-only rule was rejected because `.gitignore:25-33` deliberately keeps the action packet out of this public repository; silently requiring a commit would choose disclosure without Cole's approval. `scripts/console-packet-retention.mjs` instead requires this parent to be checked and the ledger to carry one matching positive receipt with the same receipt ID, Cole-approved destination class, ISO verification timestamp, and SHA-256 for each of the exact three packet paths. Checklist-only, ledger-only, mismatched ID/destination/timestamp, missing/changed digest, duplicate receipt, and contradictory receipt/ledger-row state mutations all fail closed; focused coverage is **12/12**. The test is wired into `mobile:store:check` and `pretest:release`. At that implementation time, records intentionally failed the new named assertion and the audit-time direct/wired board was **174 passed · 1 warning · 5 failed**; that count is historical and superseded by the current **179/1/3** board. This models the already-recorded blocker; it does not choose a destination, copy a packet, verify a backup, or establish submission readiness.
  - 🔒 **Receipt-to-bytes binding defect corrected locally, Codex 2026-08-15 23:25 CDT; independent design audit confirms this byte-binding lane; Claude audit pending.** The first guard synchronized the two records but did not compare their shared digests with the current packet bytes: its synthetic all-`a` receipt passed even though fresh hashes of all three real files differed. The checker now supplies fresh binary SHA-256 evidence for every exact packet path, and the helper requires each receipt digest to equal that evidence. Matching-but-invented digests, packet mutation after receipt, and missing current-byte evidence now fail closed; focused coverage was **15/15** and the audit-time direct board was honestly **174 passed · 1 warning · 5 failed**. That count is historical and superseded by the current **179/1/3** board. The independent audit also found the later sibling-section residual; that distinct defect was corrected in the 23:47 CDT unit below. No packet was copied, published, or represented as retained.
  - 🛡️ **Full retention-section contradiction guard implemented locally, Codex 2026-08-15 23:47 CDT; Claude audit pending.** The helper now locates the one exact checklist parent, bounds its section at the next top-level checkbox, requires the sole receipt to live inside that section, and evaluates the complete section plus the matching ledger row for contradictory current-state language. The exact pre-fix synthetic fixture changed from `sibling-red-fixture-pass=true` to `false`; a misplaced receipt and a sibling open-state claim also fail. Focused coverage was **18/18** and the audit-time direct board was **174 passed · 1 warning · 5 failed** because actual retention was still not established. That count is historical and superseded by the current **179/1/3** board.
  - 🛡️ **Strict retention-receipt timestamp semantics independently audited, Codex 2026-08-16 19:12 CDT; Claude audit pending.** Before correction, otherwise matching synthetic receipts with `2026-02-30T17:00:00Z`, `2026-08-16T24:00:00Z`, `2026-08-16T17:00:00-00:00`, and a future `2099-01-01T00:00:00Z` all verified. The helper now validates calendar and clock fields before parsing, requires a known offset, rejects future time, and parses both records against one supplied fail-closed clock. Focused controls pass **31/31**; an independent 18-case matrix accepted valid leap-day, exact-now, `+00:00`, numeric-clock, and valid-`Date` controls while rejecting impossible dates/times, `-00:00`, future time, invalid clocks, and record mismatch. The canonical parent remains unchecked with no receipt, so retention remains red and the complete board remains **179/1/3**. No destination was selected and no packet was copied, moved, deleted, published, or represented as retained.
  - 🛡️ **Inactive-Markdown retention false-pass closed locally; independent Codex audit PASS; Claude audit pending (2026-08-17 01:31 CDT).** The pre-fix helper accepted a complete matching record pair when both records were inside HTML comments or fenced code and accepted an active parent whose only receipt was commented. It now filters comments, backtick/tilde fences, indented code, and correctly terminated CommonMark raw-HTML blocks before locating records, and requires the exact active receipt-row prefix. Focused coverage passes **38/38**; an independent frozen **40/40** matrix additionally validates unclosed/longer fence boundaries, raw-HTML blank-line semantics, inactive ledgers, inline-code/link-label wrappers, active controls, and the existing byte/timestamp/duplicate/contradiction refusals. The canonical parent and synchronized receipt remain absent, so retention stays red and the board stays **179/1/3**. No destination was chosen and no packet or external state changed.
- [x] The approved `.99` catalog is consistent across displayed prices and every live billing provider: Starter `$29.99 / $299.99`, Standard `$49.99 / $499.99`, Heavy `$149.99 / $1,649.99`; Heavy Annual remains web/Google Play-only.
  - ✅ Source, the historical build-4 payload, website display, and intended Apple/Google catalog copy use the approved values.
  - ✅ **AUDIT CONFIRMED (Claude, 2026-08-13).** Under Cole's fresh approval, Codex created six immutable live recurring Stripe Price objects on the existing Starter, Standard, and Heavy products at exactly `2999/29999`, `4999/49999`, and `14999/164999` cents for month/year. Claude independently read back all six as live, active, licensed recurring prices with the intended intervals, plan/allowance metadata, and release `1.4.2`; independently matched all six Railway mappings; confirmed deployment `b030242a-0019-4c6a-9510-6ff611eb8a50` is `SUCCESS`; and verified `/health` at `ok: true`, `version: 1.4.2`, stage `full`. The former **35/0/0** live-gate wording was refuted and is superseded. The latest Apple result is **40/0/0** after reviewer readiness deployment and public privacy publication. This does not change the already audited Stripe-price evidence. The six superseded Price objects remain active but are no longer referenced by production; deactivation was not part of Cole's approval and remains a separate reversible cleanup decision.

## Public legal pages

- [x] Privacy policy contains the Operator product-specific notice.
  - ✅ **The prior 2026-08-14 06:00 CDT live-content blocker is superseded; freshly reverified 2026-08-15 16:47 CDT.** A read-only fetch of the exact public privacy URL returned HTTP 200, and the live HTML contains all three checker-required disclosures: the keyed one-way registration-request IP hash, that the raw IP address is not stored, and the no-more-than-48-hour retention boundary. This closes only public-notice content parity; it does not prove either store-console privacy declaration or resolve the separate OpenAI/transcription disclosure decision below.
  - ⚠️ **Separate OpenAI/transcription disclosure decision — no completed correction is claimed (Codex, 2026-08-14 02:03 CDT).** The desktop delivery-review path sends user-selected or recorded sermon audio to OpenAI's transcription API with the user's own OpenAI key (`electron/main.js:2438-2479`). The mobile recorder instead saves audio in local IndexedDB and exposes it only through an explicit OS share/download action (`src/mobile/TabletRecorder.tsx:127-164,206-225`; `src/mobile/recordings.ts:37-49,112-118`); no OpenAI/transcription call exists in the mobile or server sources. The checked-in public addendum names Anthropic and other provider roles but not OpenAI or transcription. Claude's earlier audit correctly left whether to name that desktop processor as a Cole/counsel disclosure decision and changed no file; the 01:47 handoff's phrase "separately recorded OpenAI/transcription correction" was inaccurate. Do not represent that item as fixed or bundle it into a publication claim until the scope decision is made and any resulting notice source is reviewed.
- [x] Terms page publishes the current mobile wording.
- [x] Account-deletion page returns HTTP 200 at the exact URL entered in Google Play.
- [x] Support page returns HTTP 200 and offers a working contact path.
- [ ] App links open every legal page from a physical device.
  - 🔴 **Candidate-bound execution required.** Complete L01–L04 in `store/mobile-physical-smoke-matrix.md` on iPhone, iPad, Android phone, and Android tablet. Each control must open its exact public HTTPS destination, load the expected page, and return to the unchanged Account surface. Static URL/source checks cannot clear this row.

## Apple

- [x] BASE1520 App Store Connect account is active and paid agreements, tax, and banking are complete.
- [x] App record `The Operator by BASE1520` exists for `com.base1520.theoperator` with Apple ID `6799805279`.
- [x] Produce and upload version `1.4.2` build `4` from the provenance-clean final source; builds 2 and 3 are superseded submission candidates.
  - ✅ **Produce independently verified by Codex; Claude audit pending.** Preserved archive `/Users/colepermenter/Library/Developer/Xcode/Archives/2026-08-13/TheOperator-1.4.2-build4.xcarchive` reports bundle `com.base1520.theoperator`, version `1.4.2`, build `4`, team `6UP72M96Q5`, arm64, and an `App.app.dSYM`. Its embedded web payload exactly matches committed `ios/App/App/public` and contains the storefront guard.
  - ✅ **Upload and local artifact custody independently verified by Codex; Claude audit pending.** Receipt `/private/var/folders/cc/3c1fmn0n53z11x36jvds29rm0000gn/T/App_2026-08-13_18-45-04.559.xcdistributionlogs` records distribution preparation with `Apple Distribution: Base 1520 LLC (6UP72M96Q5)` and `get-task-allow = 0`, then `UPLOAD SUCCEEDED with no errors`, `Upload succeeded`, and `Uploaded package is processing`; Apple-issued Delivery UUID is **`e235bb44-1507-45b8-b902-6875ca648487`**. The build-4 IPA is now preserved beside the archive at `/Users/colepermenter/Library/Developer/Xcode/Archives/2026-08-13/App-1.4.2-build4.ipa`: it is 2,191,923 bytes, passes `unzip -t`, hashes to SHA-256 `454b75751e762e5bc91548ad78fa56cd0c24a449268a07ce673e1cdfc8e0aa3c`, and is byte-for-byte identical to the still-present Xcode pipeline original. This is durable local custody, not an off-machine backup.
  - ⏳ **Processing/selectability remains unverified.** Build 6 upload is accepted: persistent Xcode logs say `UPLOAD SUCCEEDED with no errors` and `Uploaded package is processing`, but no processing-complete, TestFlight-appearance, or listing-selection receipt exists. Build 3 is still the last build independently seen as `Ready to Submit` and saved on the 1.4.2 listing. Builds 4 and 5 are historical upload evidence and must not be selected or submitted. Build 6 is the only current executable path, but attach/select build 6 only after App Store Connect shows it processed/selectable and Cole gives fresh action-time approval; do not infer that state from the upload receipt.
  - 🔴 **Fresh console-read blocker, 2026-08-14 20:57 CDT.** A read-only navigation to the exact build-5 TestFlight route resolved to App Store Connect's login URL with `authResult=FAILED`; the visible page is the blank Apple Account sign-in form, not the iOS build list. The only alternate browser family was unavailable. No credential, passkey, console field, build selection, or submission action was attempted. This proves only that the current browser session cannot read processing/selectability; build 5 remains uploaded but unverified in App Store Connect until Cole signs in for a later read-only recheck.
- [ ] Five purchasable iOS subscriptions exist in one group and match the iOS-filtered catalog in `server/src/iap-products.json`; the existing Heavy Annual record remains unavailable on iOS and is retained only for Android/web mapping and defensive receipt recognition.
  - 🟡 **Console reconciliation by Codex 2026-08-13; Claude console audit pending.** App Store Connect shows all six records in `The Operator Access`, each `Prepare for Submission`; Starter Monthly is on sale in all regions at `$29.99` in USD storefronts, and Heavy Annual remains unavailable as intended. English (U.S.) localization was saved for Starter Monthly, Starter Annual, and Standard Monthly from the canonical product rules. Standard Annual localization returned App Store Connect's generic `An error has occurred. Try again later.` Annual upfront availability remains unsaved for Starter Annual and Standard Annual, and review screenshots remain absent. App Store Connect's browser security gate then denied the final availability action, so no workaround was attempted and the parent stays open.
  - 🔴 **Fresh read-only console re-check 2026-08-13 11:30 CDT: Standard Annual is still incomplete, so no live price can honestly be reported.** Its detail page identifies `com.base1520.theoperator.standard.annual`, duration `1 year`, status `Prepare for Submission`, but `1 Year Upfront` still shows **`Set Up Availability`**, current pricing has no displayed amount, localization still shows **`Add Localization`**, and the review screenshot remains absent. The subscription-group route then returned an empty shell with a page error, so the other four intended prices were not claimed from stale history. No field was edited or saved.
  - ✅ **Console action packet locally reconciled.** Both Apple and Google tables match the approved `.99` catalog, Heavy Annual is explicitly non-purchasable on iOS, and automated row-level checks prevent pricing/scope drift. `store/console-action-packet.md:53-57` no longer presents an unsupported SKU as transcribable fact; it requires reading the existing SKU and Primary Language in App Store Connect without inference or overwrite. The named guard continues to pass in the current **167/2/1** control. Its isolated asserted-SKU mutation is recorded below; Claude audit of this new guard is pending. This validates the packet, not the live console values.
  - ✅ **Existing-group duplication hazard closed locally by Codex 2026-08-16; independent read-only audit passed; Claude audit pending.** The action packet now says to use the already-existing subscription group `The Operator Access`, forbids creating a second group, and requires re-reading live membership before any approved edit. The existing catalog assertion requires those boundaries and rejects the stale `Create **one** group.` instruction. The real-tree checker remains intentionally red at **174/1/5** on the same five release blockers; the isolated exact regression in `/private/tmp/operator-console-group.rBZHGK` adds only the named existing-group assertion at **173/1/6**. Independent review reproduced both boards and found no material residual in this scope. This proves local transcription safety, not current live group membership, product completion, StoreKit return, purchase behavior, or submission readiness.
  - ✅ **RESOLVED 2026-08-13 (Cole): plans now render on the account page** after the App Store Connect records were completed. **The prediction recorded below held exactly — no app change was made**, confirming the root cause was console product state, not app code. ⬜ **Still to verify (needs Cole's eyes):** the rendered amounts come from **StoreKit**, so they prove what is configured in ASC — they must read **$29.99 / $49.99 / $149.99** on monthly with **three** cards, and **$299.99 / $499.99** on annual with **two**. Wrong amounts mean the ASC products were created off-catalog.
  - 🔴 **Original finding — CONFIRMED FAILING ON DEVICE 2026-08-13 (Cole, iPhone + iPad): only Heavy Monthly was offered.** Expected three on the monthly tab (`starter`, `standard`, `heavy`) and two on annual (`starter_annual`, `standard_annual`); one renders. **This is the single largest commercial defect on the board** — it hides every cheaper entry point and presents The Operator as a $149.99/month-only product.
  - **Diagnosis (Claude, source-verified).** The app requests all five iOS IDs (`store.ts:90-94`), the cycle filter is correct (`MobileApp.tsx:1351`), and native purchase buttons come **only** from StoreKit-returned products (`MobileApp.tsx:1678`). So four products are not being returned. Root cause is App Store Connect product state, not app code: **a subscription in `Missing Metadata` is not returned by StoreKit.** Four IDs to check — `com.base1520.theoperator.starter.monthly`, `.starter.annual`, `.standard.monthly`, `.standard.annual`.
  - ✅ **Silent partial-catalog defect AUDIT CONFIRMED (Claude, 2026-08-13); fresh destructive reproduction 2026-08-14.** `src/mobile/storeCatalog.ts` resolves every requested platform product and throws an `IncompleteStoreCatalogError` containing matched/expected counts plus exact missing plan names when any product is absent. Claude explicitly confirmed the 8/8 control and independently disabled the guard, which failed by exact plan name. A fresh isolated copy printed `if (false && missingPlans.length > 0)` and again failed the three named partial-catalog properties at **5/8**; restored repository source returned to **8/8**.
  - ✅ **Visible fail-closed UI source audit confirmed; uploaded build-4 presence independently verified.** `src/mobile/store.ts` cannot render until the complete-catalog check succeeds; load failure clears old cards, and native account UI shows `STORE PLANS ARE TEMPORARILY UNAVAILABLE` rather than a partial catalog. The 8/8 control covers both production wiring and visible fallback. A fresh binary-string check found both that warning and `IncompleteStoreCatalogError` in the preserved build-4 archive's `public/assets/index-B7_22Cnp.js`. This proves the guard is in the historical upload, not that StoreKit now returns all products, that a purchase succeeds, or that build 4 contains the later tablet fix.
  - 🧪 **Falsifiable prediction on record (Claude, 2026-08-13).** The build on the iPad is **development-signed** (`Apple Development: Adam Permenter`), so it queries **sandbox StoreKit**, which only returns subscriptions at `Ready to Submit` or beyond. Completing the four records to that state should restore three plans on the monthly tab and two on annual **with no app change**. If it does not, this diagnosis is wrong and the next suspect is the subscription **group** localization.
- [ ] StoreKit sandbox purchase, renewal, cancellation, restore, and account-switch tests pass.
- [ ] App privacy answers match `store/privacy-data.md`.
  - ✅ **Public notice source parity AUDIT CONFIRMED; public publication independently verified 2026-08-14 14:34 CDT.** `website/operator-privacy-addendum.html:8` discloses the keyed one-way registration-request IP hash, that raw IP is not stored, its registration-abuse/fraud purpose, and the no-more-than-48-hour retention boundary already declared in `store/privacy-data.md:12,32,48` and implemented in `server/src/account-registration.js:28,64-65,94-100`. The actual live checker requires all three disclosure phrases. Claude published the checked-in section under Cole's authority; Codex and a separate read-only verifier then reproduced HTTP 200 and all three predicates. After WordPress apostrophe normalization, the live section exactly matches the checked-in source (2,761 characters; SHA-256 `dd1a570044f7cb0f2e502661f91d475df62b13ba5fe0be60f8104566f9c633bd`). Apple is **40/0/0**; Google is **38/0/1**, failing only purchase verification. This closes public notice parity, not either store-console privacy declaration.
- [ ] Age rating, category, Content Rights, SKU, Primary Language, copyright, support URL, privacy URL, and review contact are complete.
  - ✅ **Copyright exact-value gap guard AUDIT CONFIRMED (Claude, 2026-08-15 02:21 CDT; Codex independently reverified).** The packet truthfully says only that a copyright was saved historically; its exact string is absent and cannot be transcribed or verified locally. Before the guard, replacing that row with `2026 Base 1520 LLC | Complete; live console saved.` or deleting it entirely left the real checker unchanged at **171/2/3**. The new row-scoped assertion requires the historical-source qualifier, exact-value absence, non-transcribable boundary, current-console read, year/rights-holder validation, non-secret recording instruction, and rejects contradictory completion language. Claude independently deleted the row, replaced it with that plausible value/completion claim, and appended contradictory completion text while preserving the honest phrases; each failed only the named copyright assertion. After the separate migration-record guard added one pass, Codex replayed all three mutations at **172/2/4** against a restored **173/2/3** control. No copyright value was guessed, and no App Store Connect field changed.
  - 🟡 **Local Apple console packet reconciled 2026-08-14; no console completion claimed.** `store/apple-console-completion-packet.md` identifies the listing text, categories, URLs, review-note draft, privacy-answer source, and product source that are locally ready to compare. It also records the exact local gaps: no independently verifiable current SKU or Primary Language in canonical metadata/the completion packet, no age-rating questionnaire/result, no locally verifiable copyright string (despite the ledger's prior console-save report), and no explicit App Review contact name/email/phone block. This checkbox stays open until those inputs are recorded and the live console is re-read. The App Review row now names only a one-time device-link code, and the shared gate enforces both that term and the absence of access/comp-code terminology. That terminology unit's 05:12 control was **160 · 2 · 1**; a printed isolated terminology regression failed the named assertion at **159 · 2 · 2**. **Claude audit pending.**
  - ✅ **Required Content Rights lane AUDIT CONFIRMED by Claude at 05:35 CDT.** Apple's current App Information reference says apps that contain, show, or access third-party content must have the necessary rights or otherwise be permitted to use it. The packet cites `store/review-notes.md:26-28` and `website/operator-terms.html:32-36` as the local basis: the initial build uses public-domain translations that BASE1520 is authorized to distribute. It explicitly says the live console declaration is unproved. Codex and Claude independently reproduced the **161 · 2 · 1** control and verified both destructive row mutations fail the named Content Rights assertion at **160 · 2 · 2** beside the unchanged screenshot hold.
  - ✅ **SKU and Primary Language gap guards AUDIT CONFIRMED by Claude at 06:04 CDT; unsafe action-packet supersession fixed locally, new guard audit pending.** Apple's current required-properties reference lists both under App Information, while `store/metadata.json` and the completion packet provide no independently verifiable current values. Claude confirmed the existing gap guards and found that the older action packet still asserted a specific SKU. `store/console-action-packet.md:53-57` now removes that assertion and directs the operator to read the existing console values without inference, overwrite, or transcription from older copy. `scripts/check-mobile-store-readiness.mjs:224-232` makes that rule load-bearing. That unit's restored control was **164/2/1**. In isolated tree `/private/tmp/operator-console-identity.ITROqX`, the printed mutation reintroduced `**SKU:** UNSOURCED-TEST-SKU`; the actual checker changed to **163/2/2**, adding exactly `Console action packet requires console-read SKU and Primary Language instead of an asserted value` beside the unchanged screenshot hold. The exact live SKU and Primary Language remain unverified; no console value changed.
  - ✅ **Age-rating console-gap guard AUDIT CONFIRMED by Claude at 07:50 CDT (implemented by Codex, 2026-08-14 07:32 CDT).** The completion packet already recorded Apple’s required questionnaire and the absence of both answers and a resulting rating, but the shared checker ignored that row. Before the fix, the actual checker stayed **165/2/1** after packet line 45 was changed and printed as `| Age-rating questionnaire | 4+ | Complete; live console saved. |`. The new named assertion requires the unresolved questionnaire/result wording, its current-build next step, and rejection of completion language. With the source restored, the repository is **166/2/1**; the same printed false-completion mutation is now **165/2/2**, adding exactly `Apple completion packet records age rating as an unresolved questionnaire-derived console gap` beside the unchanged screenshot hold. Claude independently re-read the honest unresolved row and current **167/2/1** gate before issuing the audit verdict. This proves local gap integrity only—not the answers, rating, App Store Connect state, or Apple submission readiness.
  - 🧪 **App Review contact console-gap guard implemented locally; Claude audit pending (Codex, 2026-08-14 07:46 CDT).** The completion packet already said no explicit App Review name/email/phone block exists, the general support email is not proof of the intended contact, and Cole must supply or approve the contact, but the shared checker ignored that row. Before the fix, an isolated mutation preserved every required gap phrase and appended `Contact complete; live console saved.`; the actual checker stayed **166/2/1**. The new row-scoped assertion requires all four approval/privacy phrases and rejects contradictory completion/configuration language. Restored repository control is **167/2/1**; the same printed mutation is **166/2/2**, adding exactly `Apple completion packet records App Review contact as an explicit approval-bound console gap` beside the unchanged screenshot hold. No contact value or PII was added, and no console completion is claimed. Codex does not mark it audit confirmed.
  - ✅ **Subscription-description copy audit-confirmed by Claude 2026-08-13; current length remeasured 2026-08-14.** The current 2,568-character staged Apple description contains all eight checked Guideline 3.1.2 elements: titles, prices, billing periods, auto-renewal, cancellation instructions, Terms, Privacy, and explicit URLs. Claude independently fetched Terms, Privacy, account deletion, and support at HTTP 200. It still must be entered and saved in App Store Connect under Cole's action-time approval.
- [ ] App Review can link to the dedicated reviewer account with one unused private device-link code, and the review path has been rehearsed from the TestFlight build.
  - 🔴 **No retrievable reviewer code is recorded, freshly rechecked 2026-08-13 22:38 CDT.** The 2026-08-10 provisioning record remains the governing account record, but the named private output file is absent and an existence-only scan found no matching top-level `operator-app-review-access-*.json` artifact under either recorded temporary-file root. This proves only that the canonical local code artifact is unavailable; it does not prove that no valid link exists elsewhere or that the production account still exists now. Under fresh action-time approval, confirm the recorded account, issue one new device-link code, keep it out of Git/chat/logs, privately place one unused code in App Review Information, and rehearse the link/study/purchase/restore path. Account-deletion testing must not destroy the account Apple is expected to use.
  - 🧪 **Reviewer-code availability wording corrected and mutation-guarded locally; independent read-only audit passed, Claude audit pending (Codex, 2026-08-16 00:58 CDT).** `store/apple-console-completion-packet.md` now limits its claim to the dated 2026-08-13 existence-only artifact scan and the absence of a code from checked-in non-secret sources; it explicitly says current account existence and code availability were not re-queried. The existing App Review device-link assertion now consumes that source boundary, preserves the fresh-approval and secret-handling instructions, and rejects the stronger stale sentence `no retrievable unused device-link code is locally available`. Restored canonical control is **174/1/5**. In isolated tree `/private/tmp/operator-console-group.rBZHGK`, restoring only that stale row changes the board to **173/1/6**, adding exactly `Apple completion packet requires a device-link code for App Review`. Independent read-only audit reproduced both boards, confirmed the fixture differs only in that row, and found no material residual. No account, code, console field, reviewer path, or submission state was queried or changed; Claude audit remains pending.
  - 🧪 **Action-packet reviewer-code state wording corrected and mutation-guarded locally; independent read-only audit passed, Claude audit pending (Codex, 2026-08-16 01:12 CDT).** `store/console-action-packet.md` no longer converts the dated 2026-08-13 existence-only scan into the present-tense claim that the 2026-08-10 codes `are no longer present`. It now records only the dated artifact absence, the checked-in non-secret-source boundary, and the fact that current account existence/code availability were not re-queried; it also requires fresh action-time approval, account confirmation, one code placed only in App Review Information, and never Git/chat/log/packet storage. The existing App Review assertion consumes both Apple packets and rejects the stale action-packet sentence without adding a pass. Canonical control is **174/1/5**; in `/private/tmp/operator-console-group.rBZHGK`, restoring only the stale action-packet lines yields **173/1/6**, adding exactly `Apple completion packet requires a device-link code for App Review`. Independent read-only audit reproduced both boards, confirmed the fixture differs only in those action-packet lines, and found no material residual. No account, code, console field, reviewer path, or submission state was queried or changed; Claude audit remains pending.
  - ✅ **Reviewer identity instruction guard AUDIT CONFIRMED by Claude at 06:50 CDT; Codex independently mutation-reproduced it at 06:57 CDT.** `server/src/iap.js:199-213` allowlists the Operator database account's email, while `src/mobile/store.ts:151-162` sends only `accountId` as the store account token and contains no email reference. `store/console-action-packet.md:110-116` and `store/review-notes.md:14` therefore correctly require the Operator email in `IAP_SANDBOX_ACCOUNT_EMAILS` and separately require the store's sandbox/test purchaser identity; they do not require both systems to use one email. `scripts/check-mobile-store-readiness.mjs:211-222` ties the instruction to both source facts. Restored control is **165/2/1**. In fresh isolated tree `/private/tmp/operator-reviewer-identity-audit.hSE56d`, the old same-email line was printed; the unchanged checker became **164/2/2**, adding exactly `Reviewer instructions separate the Operator allowlist identity from the store purchaser identity` beside the unchanged screenshot hold. This proves local instruction integrity only; tester enrollment, account allowlisting, device linking, and a sandbox purchase remain unproved.
- [ ] 🔴 Final iPhone and iPad submission screenshots come from the production-backed release candidate and pass Apple format checks without provider errors.
  - ✅ **All five currently staged files pass Apple's structural requirements.** iPhone files are `1284×2778`; the stale iPad files are `2064×2752`; every file is 8-bit RGB without alpha. Apple's current 13-inch matrix also accepts the build-5 device's native `2732×2048` landscape captures ([Apple screenshot specifications](https://developer.apple.com/help/app-store-connect/reference/app-information/screenshot-specifications/)).
  - ✅ **The two iPhone files were recaptured and independently reviewed; Claude marked the 20:55 unit AUDIT CONFIRMED at 21:01 CDT.** Commit `ea6eedf` changes only those PNGs atop uploaded build-4 payload commit `550d708`; their committed and working hashes match. Visual review found real Romans 8 start/study content with no loading state, provider error, personal email, notes, device ID, access code, account data, or price claim. Claude disclosed the local Electron/source/API lineage. Codex independently verified the harness's local URL, shipped-origin rewrite, viewport, and `capturePage()` path; after the disclosed Lanczos conversion, each scratch capture is pixel-identical to its committed counterpart (`AE = 0`). Those renderer files matched the packaged build-4 tree at capture time; the current post-`ac04383` iOS public bytes no longer match the preserved archive.
  - 🔴 **The three canonical iPad files remain stale, but two intended build-5 slots now have clean full-screen originals.** Their Aug-10 staged files still predate current source and `ios-ipad-submission/01-infinite-sermon-desk.png` still renders the superseded `SAVED ON THIS IPAD` wording. At 15:29 CDT a read-only device query confirmed installed Operator identity `1.4.2 (5)`. At 15:50 CDT Codex independently measured and visually inspected `/Users/colepermenter/Downloads/IMG_0158.PNG`–`IMG_0160.PNG`: all three are 2732×2048 opaque RGB PNGs; `IMG_0158` is a clean full-screen SERMON desk and `IMG_0159`/`IMG_0160` are two clean full-screen PLAIN-study views. None shows active Preach Mode, so they supply only two of the three intended slots and are not staged as a partial replacement.
  - ✅ **Native iPad landscape acceptance AUDIT CONFIRMED by Claude at 16:25 CDT (implemented by Codex, 15:50 CDT).** `scripts/screenshot-provenance.mjs` defines the shared 2732×2048 landscape target and the existing Apple-accepted 2064×2752 portrait form; the real readiness checker consumes that shared contract. Focused control is **7/7** and, at the time of that audit before the next-build PREACH source edit, the complete static gate was **171/2/1**, failing only the intentional visual hold. In isolated fixture `/private/tmp/operator-ipad-dimension-mutation.NgYeBN`, Codex's printed source mutation changed the load-bearing width to `2731`; the named `native build-5 iPad landscape captures match the checker dimensions` test failed at **6/7**. Claude independently verified the shared contract and checker consumption, then reproduced `2732 → 2731` in a separate scripts-plus-store fixture at **6/7** before issuing the verdict. This proves exact dimension acceptance, not screenshot provenance or visual suitability.
  - ✅ **The remaining screenshot provenance failure is fail-closed in automation.** `store/screenshots.md` retains the Apple submission hold, and the shared control requires that hold while this canonical screenshot row remains unchecked. This catches record drift; it still cannot read pixels. **Build 6 now exists as a source-bound uploaded artifact, but no final PLAIN / SERMON / PREACH set is proven against its packaged UI. Treat every build-5 frame as a historical visual draft; reproduce the complete set from build 6 or prove pixel-equivalence of any reused draft against packaged build 6, then stage, visually recheck, clear the hold, and rerun the complete gate only for the build-6-proven complete set.** App Store processing/selectability remains a separate listing gate and is not established by the upload or device receipts.
  - 🧪 **Apple positive screenshot-receipt guard independently reviewed and custody-hardened (Codex, 2026-08-17 06:36 CDT; independent final PASS).** The single Apple screenshot assertion requires `store/apple-screenshot-verification.json` to be `verified` with a visual `PASS` bound to immutable tagged build-6 candidate `729f98c38b058b355f9937b3be3def16cd5e86d7`, candidate-derived bundle/version/build identity, an identified reviewer with a valid non-future offset timestamp, and the exact five unique canonical live path/hash pairs. The receipt must also satisfy committed/pushed Git custody. The canonical receipt remains `hold`, untracked, and custody-invalid. Focused provenance is **35/35**. This binds a declared review to bytes and immutable candidate identity; it does not cryptographically prove capture from the package or replace physical/candidate visual review.
  - 🔴 **First build-6 Preach capture attempt rejected, 2026-08-17.** `IMG_0163.PNG` and `IMG_0164.PNG` are structurally valid `2732x2048` opaque PNGs, but independent visual review confirmed a workspace/manuscript reference mismatch and unsuitable personal/copy content. Their SHA-256 values are `bbace3938d530a91042d3ace3caec68fd96e289af4d517d569e158f845a4709a` and `bf76d374d1255dafe9e99558b0004bae408516f83a7eaf7d6c622a1b142f9bed`. Neither file was staged or accepted; the complete build-6 PLAIN / SERMON / PREACH set and visual receipt remain open.
  - 🟡 **Ten-frame recapture intake leaves two unacceptable slots, 2026-08-17.** All files pass the 2732×2048 opaque 8-bit RGB structure. `IMG_0166.PNG` is the strongest content-safe PLAIN candidate. `IMG_0167.PNG` and `IMG_0170.PNG` visibly omit verse 13 between their `22:10–12` and `22:14–17` divisions. `IMG_0174.PNG` is active Preach Mode with a matching reference, but is rejected for family-identifying manuscript copy and a footer-clipped bottom line. The files remain unstaged and the parent remains unchecked until a clean SERMON frame avoids or follows correction of the division gap, a clean unclipped active-Preach frame exists, and the complete build/source/byte-bound review passes. Exact hashes and per-frame verdicts are in the local intake artifact.
  - 🟢 **Current device identity reconciled without retroactive screenshot provenance, 2026-08-17 13:12 CDT.** A later bundle-filtered read now independently reports the connected iPad's installed Operator metadata as `1.4.2 (6)`, superseding build 5 as the device's current version/build identity. It does not prove the distribution channel or bytes and cannot retroactively establish which installed bytes produced the earlier capture files because no contemporaneous identity receipt was taken during those captures. The Apple screenshot hold and complete candidate/source/byte-bound visual review therefore remain open.
- [ ] Archive validates in Organizer without critical warnings.
  - ✅ Historical builds 4 and 5 have matching archives and accepted-upload evidence. Current build 6 declares `CURRENT_PROJECT_VERSION = 6` in Debug and Release, has a matching source-bound archive/export receipt, and Apple accepted its upload without errors. Keep this row open because a distinct Organizer validation receipt and App Store processing-completion/selectability evidence are absent.
- [ ] TestFlight external review passes before production submission.
  - 🟡 **Submitted / pending Beta App Review (Cole-reported, Claude-witnessed, 2026-08-17).** Cole reported five external testers enrolled and build `1.4.2 (6)` submitted to TestFlight Beta App Review. Codex could not independently reread the authenticated console. This parent remains unchecked until Beta App Review passes; enrollment/submission does not prove the dedicated reviewer account/device-link rehearsal, sandbox purchase, App Store version-page build selection, or production App Review submission.

## Google Play

- [x] Correct BASE1520 Play Console account access is restored; Cole signed in as `cole@base1520.com`, and Claude first-hand observed the verified Base 1520 organization account, developer-verification green banner, and `The Operator` draft (app ID `4973416156802794205`, zero uploads) on 2026-08-14. This closes account access/ownership and developer verification only; payments-profile readiness and every product/release field remain separately open.
  - ⚠️ **Historical blocked-session check, superseded by Cole's verified-account sign-in (2026-08-14 09:13 CDT):** opening the Play Console developer root in the then-available browser identity resolved to the developer-account signup boundary rather than the existing dashboard. No signup occurred. The later correct-account session closes that access blocker without changing the historical observation.
  - 🔍 **Fresh ownership-trail check 2026-08-13 23:22 CDT:** the connected Gmail profile is the same personal Google identity shown on the signup page, `aperme2@gmail.com`. Narrow searches across the Aug-7–14 creation window and all-time subject/sender history found no Play Console invitation, developer-account confirmation, or `Play Console` subject in that mailbox; the only messages from Google's consumer Play sender were unrelated terms/privacy notices. Exact Spotlight searches of the local Mail store also returned no Play Console match, but an empty local index is not proof about an unconnected or unindexed mailbox. These focused searches found no observable ownership or invitation email in the currently connected personal mailbox; they cannot exclude different wording, a deleted or excluded message, or an invitation without a retained email. They do **not** identify the true owner or prove that `cole@base1520.com` or another Google identity was not used. The next console read must use the identity that created or was invited into the existing developer account.
  - ⚠️ **Historical owner-candidate evidence, superseded by the console sign-in (2026-08-13 23:39 CDT):** Claude found Google Play developer notices addressed to the candidate identity while Codex's different Gmail connector could not see that mailbox. Email receipt alone was not proof; the later real-console session supplies the access evidence.
  - ✅ **Console access supersedes the earlier identity hunt (Cole/Claude, 2026-08-14 14:46 CDT).** Cole signed into the real verified Play developer account; Claude observed the organization and draft app directly. No duplicate account was created. This is sourced console-access evidence, not proof that payments, Play App Signing, products, declarations, testers, or a release are configured.
  - 🔴 **Fresh current-session Play Console boundary, 2026-08-16 02:18 CDT; read-only.** The 2026-08-14 authenticated read remains valid evidence for account identity, ownership, developer verification, and the zero-upload draft, but it is not current-session evidence. A direct read of `https://play.google.com/console/` in the available in-app browser resolved to `/console/signup`, titled `Creating a developer account`, under the personal browser identity. The visible page offered account-type creation and `Switch accounts`; it did not show the verified Base 1520 organization dashboard or `The Operator`. No account type, `Get started`, account switch, credential, or console action was used. This proves only that this browser session cannot read the existing organization account. Future console inspection must use the already-verified organization identity; do not create a duplicate developer account. Package binding, payments, Play App Signing, products, declarations, testers, AAB, purchase, and submission remain unverified.
- [x] Google Play app record `The Operator` exists as a zero-upload draft. The console's package cell was ellipsized, so do not claim the full bound package from that read. Local `android/app/build.gradle:19,22` independently declares namespace/application ID `com.base1520.theoperator`, and no local `.aab` exists; verify the bound package at the first AAB upload before proceeding.
- [ ] A new upload key is securely created or the existing key is recovered; encrypted backup and fingerprints are recorded outside Git.
  - 🔴 **Configuration/recovery state freshly verified 2026-08-14 00:45 CDT.** The expected properties file and default keystore are absent, all four supported environment inputs are absent, and the offline release dry-run, using the Java/Android SDK/cache environment pinned by the `mobile:verify:android` package script, configured the app and failed closed in `android/app/build.gradle` before any bundle task executed. No key contents, password, alias value, or private-key hash was read. Creation or recovery, fingerprint recording, and encrypted off-machine backup remain one action-time credential unit; none was attempted here.
  - 🔴 **Current value-free inventory reconfirmed 2026-08-15 22:58 CDT.** Zero supported upload-input names are present, both expected local configuration/key paths are absent, and the Android tree contains no `.jks`, `.keystore`, or `.aab`. This existence-only check does not search an unconfigured external backup and does not authorize key creation or recovery.
- [ ] Play App Signing is enabled.
- [ ] Subscription and six base plans match `server/src/iap-products.json`.
- [ ] License testers pass purchase, renewal, cancellation, restore, pending purchase, and account-switch cases.
- [ ] Data safety, content rating, target audience, ads declaration, app access, and account-deletion URL are complete.
  - ✅ **Local Google console packet AUDIT CONFIRMED (Claude, 2026-08-14 03:21 CDT); no console completion claimed.** `store/google-console-completion-packet.md` maps the locally ready listing copy, category, 18+ audience intent, support/privacy/deletion URLs, review instructions, Data safety source, no-ads evidence, product mappings, and structurally ready icon/feature/phone assets. It references `store/products.md` and `server/src/iap-products.json` rather than copying prices, Google product IDs, or base-plan IDs. Codex independently reproduced the exact package, the intentional 158/2/1 screenshot-hold gate, and seven anti-drift assertions; temporary price and base-plan duplications were printed and each made the same control fail. The shared review steps route the secret to the reviewing store's private reviewer-access field instead of naming Apple's field for both stores. This row remains open for the live ads/access/audience/Data safety answers, an IARC questionnaire/result, News and any other presented declarations, one private reviewer code, and authenticated console proof.
- [ ] Phone, 7-inch, and 10-inch tablet screenshots plus icon and feature graphic are uploaded.
  - ⚠️ **Source prerequisite fixed locally; recapture still required.** The rebuilt bundle now says `SAVED ON THIS TABLET`, but the existing submitted candidates still visibly contain `SYNC PENDING`, clipped status text, a commentary error, and the old iPad wording. Do not upload them.
  - ⛔ **Recapture environment verified blocked 2026-08-11.** Android platform tooling is installed, but no system image/AVD exists; installing one requires a large download and licence acceptance. Browser-rendered frames from the same production bundle are an explicit capture-method decision, not yet accepted as device evidence.
- [ ] Internal test track install passes from Google Play, not a locally installed APK.

- [x] Push the reviewed Windows CI and 1.4.2 source commits after repairing GitHub authentication.
  - ✅ **Completed and remotely verified 2026-08-12.** Cole approved the credential repair and push; GitHub CLI now has `workflow`, and `origin/main` resolves to local `HEAD` `1183459e133146ccf7999dd2733d3780bd012efe`. The unrelated local `.claude/launch.json` change remained uncommitted. The Windows workflow requires a version tag or a manual dispatch with an existing tag, so this source-sync check does not claim a signed Windows artifact exists. The already-published historical name exposure and any history rewrite remain separate, unresolved decisions.
  - ⛔ **Signed-build dispatch is not yet safe.** Fresh local/remote inspection shows existing tag `v1.4.2` still points to `b96313f`, while the reviewed Windows workflow and fixes now end at `1183459`; GitHub has no Windows workflow runs. Manual dispatch with `v1.4.2` would check out the old tag, not current `main`, and the existing 1.4.2 release is already public, so the workflow's upload step would attach assets directly to that public release rather than its intended draft boundary. Do not dispatch until Cole chooses a separately reviewed tag/release strategy.
  - ✅ **Public-release overwrite guard audit-confirmed by Claude.** The workflow now queries `isDraft` and exits before any upload when an existing release is public. `scripts/test-windows-release-workflow.sh` is wired into `scripts/release.sh`. Claude independently proved three mutations fail, including moving `exit 1` after upload—the ordering failure a presence-only check would miss. This prevents accidental public-asset replacement but does not make stale `v1.4.2` safe to build.
  - ✅ **Release-tag version provenance guard audit-confirmed by Claude.** `scripts/release.sh` refuses to continue when the working package version differs from the version committed at `HEAD`, before GitHub release reads, credentials, builds, or publication. The focused regression exercises a committed match and an uncommitted version bump, and requires the guard before every consequential release phase. Codex killed inverted-comparison and removed-invocation mutations; Claude independently killed a warn-only mutation and verified real behavior and ordering. No published tag was moved. That audit covered committed version provenance; exact remote-tag targeting and the distinct clean-working-tree property are separately scoped below.
  - ✅ **Exact remote version-tag target AUDIT CONFIRMED by Claude; branch and clean-tree properties remain separately scoped (2026-08-14 23:25 CDT).** The split flow depends on Windows CI creating an existing tagged draft before `release.sh` builds the Mac half; there is no `gh release create --target` path. After a fail-closed remote refresh, `scripts/check-release-provenance.sh` requires clean local `HEAD`, fetched upstream, and their package versions to agree, then reads the exact remote `v$VERSION` ref and requires its dereferenced commit to equal both. Annotated tags use the remote peeled `^{}` target; lightweight tags use the direct target. The disposable bare-remote fixture proves missing-tag refusal, correct annotated and lightweight passes, fetch-failure refusal, dirty tracked/staged/untracked refusal, uncommitted and committed-unpushed version refusal, and same-version stale lightweight and annotated tag refusal. Claude independently ran control and bypassed the exact tag equality in isolation; the stale same-version case failed by name. Codex freshly reproduced that mutation in `/private/tmp/operator-tag-audit-recheck.xcj1z1`. This audit promotion applies to exact remote-tag binding, not the separate dirty-checkout or post-build recheck properties. No real tag, remote, release, credential, or build state changed.
  - ✅ **Post-build provenance recheck AUDIT CONFIRMED (Codex 2026-08-14 23:27 CDT; Claude 23:35 CDT).** Claude verified real-path ordering in source (`release.sh`: notarized build 78 → Gatekeeper 94–101 → final check 107–108 → by-ID upload 111) and independently killed both mutants by exact name: deleting the final call → `Release provenance guard must run exactly twice`; moving it before Gatekeeper → `Final provenance guard must rerun after Mac build and Gatekeeper, before asset upload`. The Mac completion path previously checked the clean checkout, upstream, version, and exact remote tag only before its tests and long notarized build. A tracked source/upstream change or remote-tag drift during that interval could therefore survive until asset replacement. `scripts/release.sh` now runs the same fail-closed guard a second time after the Mac build and Gatekeeper checks and before the by-release-ID upload helper. The focused test requires exactly two invocations, the first before GitHub/credential/build access and the final one after build/Gatekeeper but before asset upload/publication. In isolated fixtures, deleting only the final call fails at `must run exactly twice`; retaining two calls but moving the final call before Gatekeeper fails at `must rerun after Mac build and Gatekeeper, before asset upload`. This closes persistent long-build drift, not the tiny check-to-upload race or the separate lack of a Windows-byte source receipt. No remote, credential, build, release, or asset state changed.
  - ✅ **Future-release Windows byte/source receipt AUDIT CONFIRMED (Codex 2026-08-15 00:08 CDT; Claude 2026-08-15 00:22 CDT — six desktop controls green; five isolated mutants killed by exact name: stale commit, replaced installer bytes, receipt-not-last, pre-upload Mac recheck, off-tag dispatch; v1.4.4 untouched and the public verifier demands no receipt of it).** The Windows workflow now requires manual dispatch from the exact tag it builds, derives the checked-out commit with `git rev-parse HEAD`, and writes deterministic `windows-source.json` evidence after final staging with repository/tag/version/commit plus SHA-256 and byte size for the installer, updater manifest, and optional blockmap. It deletes any prior receipt before payload replacement, removes a stale blockmap when the current build has none, mutates the captured numeric release ID rather than re-resolving the tag, and uploads the receipt last as the completion marker. Mac completion requires the receipt and verifies the immutable release ID, draft/tag identity, current asset counts/states/sizes/server digests, receipt coordinates, and the same commit both before credential access and again after Mac upload immediately before publication. Focused control passes; isolated commit-comparison, payload-digest, receipt-order, final-Mac-recheck-order, and dispatch-ref mutations fail by their named properties in `/private/tmp/operator-windows-receipt-mutations.dHBT2L`. This is future-release procedural byte/source binding only: public v1.4.4 was not altered and has no receipt, the co-located JSON is unsigned, an actor with release-write authority could replace both payload and receipt, and the tiny final-check-to-publish race remains.
  - ✅ **Desktop dirty-source refusal AUDIT CONFIRMED (Codex 2026-08-14 22:08 CDT; Claude 22:23 CDT, HANDOFF entry) — with an OPEN workflow-collision follow-up owned by Codex:** perpetually-dirty tracked release records (`store/*.md`) and tracked `.claude/launch.json` will trip this guard on the next real release unless records are swept into the release commit and launch.json gets an explicit index policy. The provenance check previously proved package-version and upstream-commit equality but then built the live checkout without requiring it to match that commit; an uncommitted desktop source file could therefore enter an artifact that its tag could not reproduce. `scripts/check-release-provenance.sh` now fails before GitHub access, credentials, build, or publication when Git reports any tracked, staged, or untracked repository change. Its isolated bare-remote fixture proves clean pass and separately rejects modified tracked, staged, and new untracked build inputs. A printed `--untracked-files=all` → `no` mutation fails the named untracked-input property; moving the guard below credential access and `npm run build` fails the named release-order property. Ignored build outputs and local credential files remain outside this Git-source assertion. No build, credential, tag, release, or remote state changed.
  - ✅ **Future-release Mac signed-bundle guard, canonical test-chain wiring, and named diagnostic layer ALL AUDIT CONFIRMED (Codex 2026-08-15 11:34–12:56 CDT; Claude audits 11:37, 11:44, and 11:50 CDT).** `scripts/verify-macos-app-bundle.sh` requires the final arm64 and Intel `.app` bundles to contain `Contents/Resources/app-update.yml`, first requires `/usr/bin/codesign --verify --deep --strict --verbose=4` to pass on the known Apple-signed TextEdit control, then applies the same strict check to the Operator bundle and requires the exact Operator bundle and Base 1520 team identifiers. `scripts/release.sh` invokes it immediately after the completed Electron build and before any release-name staging, Gatekeeper check, upload, or publication. The focused behavioral suite passes missing-updater, unhealthy-verifier, invalid-target-signature, wrong-team, wrong-identifier, exact-argument, and release-order cases. It is the first command in the named `test:release` script, while the release driver retains its explicit pre-build invocation; `npm run --ignore-scripts test:release` and the normal lifecycle pass, so the coverage does not depend on running the privileged release driver or npm pre-hooks. Claude independently confirmed the guard with a verifier-health bypass at 11:37, then confirmed the canonical wiring at 11:44 through a full `--ignore-scripts` run, the target-strict bypass, and a novel suite-entry removal kill. Codex closed Claude's repeated zero-output audit ambiguity without changing the boundary: structural/order and Mac call/result assertions now emit named `Desktop release verification failed: ...` diagnostics instead of exiting silently under `set -e`. Claude's 11:50 four-way audit confirmed the helper still exits nonzero, all 36 routed assertions remain load-bearing, `|| true` only lets missing discovery reach its named assertion, and the exact verifier-health bypass now emits `Desktop release verification failed: Mac verifier must execute the exact known Apple control verification call` both directly and through the canonical command. Codex independently replayed the real-tree and isolated controls plus both mutation paths in `/private/tmp/operator-mac-diagnostics-audit.JIIsd3` at 12:56 CDT with the same result. The current arm64 bundle blocks deterministically for a missing updater configuration; the current Intel bundle contains the configuration but the helper refuses to evaluate it because this Mac also fails the TextEdit control. This proves the future release and ordinary regression paths fail closed without mislabeling a broken trust evaluator as an app defect; it does not create or validate a new build, repair the installed app, prove notarization, or change public v1.4.4.
  - 🔴 **R1–R4 coupled future-desktop transaction source review HOLD (Codex, updated 2026-08-16 14:51 CDT); this supersedes any broader approval implication from the green subproperty audits above without changing their historical evidence.** Three bounded R1 defects are now closed locally and independently audited: Apple credential scope, executable two-guard grammar, and official/canonical source identity. The privileged release entry resolves its own physical path, strips inherited Bash startup/function state before plain-Bash children, and invokes the physical adjacent guard twice. The guard resolves its own physical checkout, requires repository `Base1520/Sermon-Study-Tool`, branch `main`, upstream `origin/main`, exact raw/effective fetch and push URL `https://github.com/Base1520/Sermon-Study-Tool.git`, and direct canonical-URL main/tag OIDs. Retained local-remote, URL/config/SSH redirection, reassignment, CDPATH, startup-hook, exported-function, alternate-tree, and multi-hop symlink cases fail closed; closed-PATH driver execution still proves both guards and both fail-stop boundaries. Apple credentials remain cleared before children, read only after the complete test gate, kept shell-local, and scoped only to the Mac build. R1 remains **HOLD** on the corrected ignored/live-root input boundary detailed below; the prior dependency/database-only wording was incomplete. Two R2 subproperties are locally corrected with Claude audit pending. Updater authority is parsed and pinned exactly, rejecting missing, empty, wrong, duplicate-key, extra-field, and final-component symlink cases before codesign. Codesign test transcripts now preserve `argc` plus every `%q`-escaped argv element under `LC_ALL=C`, while literal expectations independently pin count/order/options and separately quote only the temp paths. The exact pre-fix metadata unquoting mutation passed the old suite and now fails by name. R2 overall remains **HOLD** because lexical ordering/wiring checks still survive early exit, dead branches, one-architecture execution, and detached pins; the success transcript still does not reject redundant extra calls; and no receipt binds verified app/version/architecture/signature/updater facts to staged DMG/ZIP bytes. R3 substring-matches the Authenticode signer, can mutate after a stale draft snapshot, lacks a closing receipt identity read, and publishes by re-resolved tag rather than captured release ID. R4 can mutate earlier assets before discovering later missing/duplicate inputs, does not fail on duplicate remote names or recheck state before each delete/post, lets an environment variable redirect the production public verifier, and reads `/latest/` only once without proving each redirect or closing the alias check. Safe mocks reproduced delete/upload after publication and a mixed/latest-alias false pass. The exact official source identity, tag equality, credential scoping, executable top-level two-call grammar, strict control-first signature check, updater-authority semantics, codesign argv boundaries, receipt schema/hash/size, and captured-ID/upload-URL checks remain useful subproperties, not transaction approval. Correct and mutation-test every remaining blocker before any new desktop release action; no network, keychain, credential value, build, runner, signing, upload, publication, or public asset was touched.

  - 🧪 **R2 updater-authority semantics locally closed; Claude audit pending; R2 transaction remains HOLD (Codex, 2026-08-16 14:37 CDT).** `scripts/verify-macos-app-bundle.sh` now uses the already-locked `js-yaml` production parser and deep equality to require exactly `provider: github`, `owner: Base1520`, `repo: Sermon-Study-Tool`, and `updaterCacheDirName: the-operator-updater`. A quoted/reordered/commented equivalent passes; missing, empty, wrong-repository, identical duplicate-key, unexpected `host`, and final-component symlink fixtures fail before any codesign call, and the symlink refusal does not print sentinel target content. The focused desktop verifier passes independently twice. In `/private/tmp/operator-r2-mutation.2qlvHd`, weakening equality to allow extra fields makes the retained `host` fixture fail by its exact `Mac bundle verifier accepted an extra updater redirect field` diagnosis. This establishes parsed authority at the check boundary only; it does not bind that check to both architecture executions, later staging, DMG/ZIP bytes, updater manifests, or uploads, and it does not establish a clean signed/notarized/installable candidate or store readiness.

  - 🧪 **R2 codesign argv-boundary oracle locally closed; Claude audit pending; R2 transaction remains HOLD (Codex, 2026-08-16 14:51 CDT).** The focused mock previously logged flattened `$*`, so one quoted `The Operator.app` argument and two unquoted path fragments produced the same evidence. In `/private/tmp/operator-r2-argv-prefx.DOsdX8`, removing only the quotes around the metadata target passed the pre-fix suite. The mock now records literal argument count and one `%q`-escaped token per element under `LC_ALL=C`; expected control, target-verification, and metadata calls independently hard-code count/order/options and separately quote only the unpredictable temp paths. The restored focused suite passes, the full release chain exits 0, and the same unquoting mutation now exits 1 at `Mac verifier must preserve the target app as one signing-metadata argv element`. Independent exact-byte/Bash 3.2 review returns scoped PASS. This closes boundary preservation for the three required calls only; the success assertions are presence checks and do not yet reject an additional malformed/redundant call, and the other R2 execution/artifact-binding blockers remain.

  - ✅ **Executable guard logical-line follow-up independently audit-confirmed (Codex, 2026-08-16 10:00 CDT).** Bash removes unquoted backslash-newlines before tokenization, so the first zero-depth scanner could still miss split `if`/`fi` tokens. The scanner now joins physical continuations before reserved-word/depth analysis. The retained split-token mutation and independent split `while`, `&&`, `||`, and `time` variants are Bash-valid but fail the top-level guard contract; both independent auditors return PASS. This closed the bounded nonexecuting-call grammar defect only; at that checkpoint official-source identity and ignored packaged inputs remained R1 blockers. The former is closed by the later row below.
  - ✅ **Official/canonical desktop-source identity independently audit-confirmed (Codex, 2026-08-16 11:36 CDT).** `release.sh` and `check-release-provenance.sh` bind their physical source paths even through alternate-tree and multi-hop symlink invocation. The privileged release entry removes inherited Bash startup hooks, option state, and exported functions before any later plain-Bash child can re-import them, including inherited `POSIXLY_CORRECT` on macOS Bash 3.2. The guard pins `Base1520/Sermon-Study-Tool`, `main`, `origin/main`, the exact official HTTPS URL, raw/effective fetch and push URLs, and direct canonical-URL main/tag OIDs; repository/config/transport redirects fail closed. The focused provenance, Windows workflow, desktop verifier, full release suite, and three independent exact-hash/source audits pass. Direct real-tree provenance with inherited POSIX mode reaches the ordinary dirty-tree refusal before the official network query. This closes the official-source identity defect only; R1 remains **HOLD** on the corrected ignored/live-root input boundary below, and R2–R4 are unchanged. No network, keychain, build, signing, upload, publication, or public state changed.
  - 🔴 **R1 ignored/live-root input scope corrected and implementation boundary recorded (Codex, 2026-08-16 13:11 CDT).** The prior dependency/database-only description was incomplete. `package.json` packages the production `node_modules` graph and the presence-dependent ignored `resources/theology-retrieval/library.sqlite3`; `scripts/release.sh` does not run `npm ci`, so the tracked lockfile does not attest the installed bytes it consumes. The Mac build also executes Vite in the live checkout, where Vite 8.0.16 loads `.env` and `.env.local`; both files currently exist and are ignored (existence and byte counts only were inspected, never their values). `electron/**/*` would include ignored `electron/embedded-key.js` if a beta build left it behind; it is absent now, but only the example file is excluded by the package config. Vite regenerates ignored `dist`, so stale `dist` is not itself treated as a direct input, but its output can inherit those ignored live-root inputs. The tracked bundle manifest records rights/source ranges but no database byte identity; a working-tree helper records the expected 7,016,448-byte / `3acfde0f…fefc134` identity, yet the release driver does not enforce that source, staged copy, and both packaged copies match. The minimum safe closure is to capture the canonical commit, build only from a mode-0700 `/private/tmp` tree populated by `git archive` of that commit, run lock-derived `npm ci` there, reject staged environment/beta-key/unknown package roots, choose and enforce deterministic database inclusion or absence, verify both packaged architectures and ASAR key absence, and rebind final `HEAD` to the captured commit before upload. Runtime trace controls must kill live dependency/env/key injection, skipped or misplaced install, lock mismatch, database flip/symlink/copy race, packaging-root expansion, live-tree copy/build, and missing or misordered pre/post-package checks. This records the precise blocker and required proof; it does not implement or approve the release transaction.

## Backend full-release gate

- [ ] `npm run mobile:store:check:live` passes with zero failures.
  - ⚠️ **Historical Apple-scoped result, superseded.** Claude previously measured **35/0/0** before the reviewer-readiness and privacy-disclosure assertions were added. The old run still proves the Google check was genuinely excluded and the then-existing Apple/API/legal assertions passed; it is not current green evidence.
  - ✅ **Apple live gate is green — reviewer-readiness audit confirmed; privacy publication independently verified.** Current source exposes only a value-free `apple_iap_sandbox_review` capability and requires it in Apple-scoped live checks. Railway deployment `59322268-02ec-413b-8ed6-14104bb0b5ac` remains the recorded successful deployment; a fresh explicit Apple-scoped production run by Codex at **2026-08-16 01:27 CDT** is **40 passed · 0 warnings · 0 failed**. This proves the live API/page contract at that timestamp, not reviewer-account existence, tester enrollment, App Store fields, or a real sandbox purchase.
  - 🔴 **The combined row is NOT satisfied because Google remains red.** Fresh Google-scoped production measurement by Codex at **2026-08-16 01:27 CDT** returns **38 passed · 0 warnings · 1 failed**, solely `Google purchase verification is operational`; the public privacy assertions pass. API identity/version/schema/stage, all seven route probes, and every generic page identity/path check pass. The box stays `[ ]` until Google verification is configured and the full live gate turns green. An Apple-scoped result cannot establish Google readiness.
  - ✅ **Google live-result record failure-identity guard AUDIT CONFIRMED (Claude 06:36 CDT; Codex independently reverified).** The readiness checker parses this current checklist measurement, the ledger's `Live production preflight` row, and its `Google production purchase verification` row. It requires identical counts, the checklist timestamp on the purchase-verification row, and the exact sole failure `Google purchase verification is operational` independently parsed from all three records. Before the strengthening, changing only the live-preflight row to `Unrelated synthetic failure` left the complete checker unchanged at **174/1/4**. Claude independently proved both a one-row mismatch and a synchronized noncanonical label fail only `Release records agree Google live preflight is blocked only on purchase verification` at **173/1/5**, with restore **174/1/4**. Codex reproduced the one-row result in `/private/tmp/operator-google-failure-final.B3feXw` and the synchronized-label result in `/private/tmp/operator-google-audit.WW8UbZ`. This promotes record integrity only; Google purchase verification remains the sole live failure at the last measured **38/0/1**, and no fresh production or purchase evidence is claimed.
  - ✅ **Live platform scope and provider wiring fail closed; Claude audit confirmed at 08:20 CDT.** Before the scope fix, the actual-script fixture with `OPERATOR_STORE_PLATFORM=appple` exited 0 at **37/0/0** while executing neither provider assertion. `scripts/check-mobile-store-live.mjs:11-22` now accepts only `all`, `apple`, or `google`; `scripts/test-mobile-store-live.mjs:13-49` requires the typo to exit 1 by the exact `Store platform scope is valid` failure. Provider-isolation fixtures require each valid scope to pass only with its own capability and to fail with the opposite provider's capability; the strongest isolated mutations fail at **7/9** and restoration returns **9/9**. This proves local scope wiring only. Current live state is Apple **40/0/0** and Google **38/0/1**.
- [ ] Production has Anthropic, Resend, Apple verification, Google Play service-account, and RTDN credentials in the secure environment.
  - Apple/Resend configuration is now visible through public health capability flags; Google purchase verification remains degraded because the required service-account and RTDN configuration is absent.
  - `IAP_SANDBOX_ACCOUNT_EMAILS` is represented by a boolean-only reviewer-readiness capability, and production now exposes that flag as true. This proves only syntactic non-placeholder configuration; the dedicated reviewer account, tester enrollment, and a real sandbox purchase remain separate required evidence.
- [x] Release readiness endpoint reports `full`; each optional provider exposes only its own verified capability.
  - ✅ **Provider independence remains audit-confirmed; current values refreshed by Codex at 2026-08-14 13:43 CDT from public `/health`.** Production reports `version: "1.4.2"`, `releaseStage: "full"`, `schema: "operator-account-bound-auth-v8"`, `ok: true`, and `missing: []`. Capabilities remain independently truthful: `apple_iap: true` · `apple_iap_sandbox_review: true` · `google_iap: false` · `marketing_sync: true` · `esv_mobile: false` · `account_recovery_email: true`; `degraded` contains only `google_iap`.
- [x] A live Operator sign-in-code email is accepted by Resend and delivered from the configured production sender to an external inbox; the sender domain's required Resend DNS records are published and verified.
  - ✅ **End-to-end delivery confirmed 2026-08-13.** Cole triggered registration and received the six-digit code in a real inbox; the account was created and the free tier granted. Claude had already verified the live DKIM, sending-subdomain MX/SPF, unchanged Google Workspace root SPF, `full` stage, and `account_recovery_email: true`. This closes delivery, not the separate production-version or store-product gates.
- [x] Migration diagnostics report only actual leaked-code remediation changes, not historical account matches.
  - ✅ **Local fix AUDIT CONFIRMED (Codex 2026-08-14 20:34 CDT; Claude 21:02 CDT HANDOFF entry — independent 2/0 control and the printed `ids.length` mutation failing both named tests at 0/2); DIRTY-CLI DEPLOYMENT RECONCILED 2026-08-16.** `server/src/migrate.js` captures the independent device-revocation and account-downgrade `rowCount` values, stays quiet when both are zero, and logs only those scalar change counts when either write changes data. `server/src/test-migrate.js` executes the real self-running migration with queued database results rather than reimplementing either SQL predicate. Fresh control is **2/0**; an isolated visible restoration of the old `ids.length` logger again fails both named properties at **0/2**. The test is wired into server `test` and root `pretest:release`. Claude's contemporaneous 2026-08-15 10:55 execution record says successful CLI deployment `0973e6a3` carried the already-audited migrate unit; fresh Railway metadata shows that superseded deployment and current successful CLI deployment `f9702bb2` both configured `npm run migrate` as pre-deploy, and server-filtered logs for both contain `schema applied` without the retired false `minted from leaked codes` event. This proves migration execution and corrects the prior false deployment-state record; it does not supply a reproducible Git-source receipt or prove a changed-row branch. The current source remains uncommitted and absent from `HEAD`/`origin/main`, and no changed-row production event is claimed. The shared checker now requires this audit, dirty-CLI deployment, uncommitted-source, and no-overclaim boundary to agree with the ledger.
- [ ] Apple App Store Server Notifications and Google RTDN reach the production backend.
- [ ] Receipt verification rejects wrong bundle/package, unknown products, replay, revoked transactions, and mismatched accounts.
  - 🔍 **Source audited sound by Claude 2026-08-11; live proof outstanding.** `iap.js` verifies the JWS against Apple roots with bundle-ID binding (`:76-82`), normalises environment off the enum (`:475`), gates sandbox behind an allowlist that fails closed (`:206-214`), blocks bearer-claim and cross-account rebinding (`:391-421`), and closes TOCTOU with a sorted per-purchase advisory lock plus `FOR UPDATE` (`:424-452`). Both previously untested load-bearing lines now have independently mutation-proven regressions. **Stays unchecked** because proving actual store rejection needs live sandbox transactions and store products that do not exist yet.
- [ ] Usage allowances reset monthly for annual plans without becoming one annual bucket.
  - ✅ **Local behavior AUDIT CONFIRMED (Claude, 2026-08-12 14:2x CDT); production proof remains open.** `billingPeriodFor` derives a new anniversary-aligned key each month and the focused suite is wired into both server and release tests. Claude independently collapsed the month offset to zero and measured 1/4, with the annual-bucket, anniversary-reset, and month-end-anchor properties all failing by name. A fresh 2026-08-14 isolated reproduction printed the same `let months = 0` mutation, again failed those three properties at 1/4, restored the repository source, and returned to 4/4. Keep the parent unchecked: live behavior against an annual production subscriber remains unverified.
- [ ] Fully refunded Stripe top-ups revoke only the unused purchased balance exactly once; partial refunds and webhook replays cannot over-revoke studies.
  - ✅ **Local fix AUDIT CONFIRMED (Claude, 2026-08-12 11:3x CDT); production gate still open.** PaymentIntent-linked transactional revocation, fail-closed unlinked grants, exact-row validation on both account debit and refund ledger writes, bounded retry, durable unmatched-event escalation, and an unapplied migration file are present. Codex mutation-proved the required PaymentIntent, full-refund gate, PaymentIntent-only lookup, and zero-row ledger rollback. Claude then independently mutation-tested all six design properties inside `revokeOperatorTopUpRefund` only — partial-refund acceptance (25/1), dropped `refunded_at` idempotency (24/2), dropped balance clamp (22/4), immediate escalation (25/1), customer-instead-of-PaymentIntent lookup and removed `BEGIN` (both abort) — all six caught, restored baseline 26/0, and reviewed the migration as re-runnable with a correctly partial unique index. **The parent box stays unchecked: the production migration/backfill, deploy, and live webhook proof remain open and are Cole's calls.**
- [ ] Stripe top-ups grant studies only when Checkout reports payment as paid; delayed methods grant on `checkout.session.async_payment_succeeded`, never on an unpaid completion.
  - ✅ **Local fix AUDIT CONFIRMED (Claude, 2026-08-12); production proof remains open.** Claude independently deleted the paid predicate and then removed the correctly targeted top-up async-success arm; each mutation failed its named property at 26/27. A fresh 2026-08-14 isolated reproduction printed each weakened dispatch, again failed the unpaid-completion and delayed-success properties respectively at 26/27, restored the repository dispatch with both guards, and returned to 27/27. Keep the parent unchecked: deployment and live immediate/delayed Stripe webhook proof remain open.
- [ ] Stripe top-up ledger insertion and account credit are one transaction; a failed or zero-row credit remains safely retryable, cannot log success, and cannot leave a permanent zero-study purchase record.
  - ✅ **Local fix AUDIT CONFIRMED (Claude, 2026-08-12); production proof remains open.** Claude's explicit 11:1x verdict authorized this promotion after independently removing `BEGIN` and `COMMIT` from `creditTopUp` only and measuring 17/25. A fresh 2026-08-14 isolated reproduction printed the transaction-free function, failed 8 tests at 19/27, restored the repository with exactly one `BEGIN`, one `COMMIT`, and two `ROLLBACK` call sites in `creditTopUp`, then returned to 27/27. Deep-equality assertions read the recorded transaction order for success (`BEGIN→COMMIT`), zero-row rollback (`BEGIN→ROLLBACK`), and winner/replay (`BEGIN→COMMIT→BEGIN→ROLLBACK`). Keep the parent unchecked: the migration, historical-row reconciliation, deployment, and live webhook/purchase proof remain open.
- [ ] Desktop access-code redemption adopts anonymous install data into the resolved account before issuing a bearer; previously redeemed installs self-heal once without trusting `x-install-id`.
  - ✅ **Local fix AUDIT CONFIRMED by Claude at 12:46 CDT; migration and deployment independently verified by Codex at 13:43 CDT; field retest pending.** `server/src/redeem.js:7-26,36-119` holds code/account resolution, four-table adoption, device-token issue, and success response in one transaction for both fresh redemption and prior-use reissue. `server/src/install-data-adoption.js:4-13,24-69` marks newly issued devices and transactionally repairs an unmarked legacy bearer once before any route; `server/src/auth.js:50-80` binds that repair to the install stored on the authenticated device, not the caller header. Schema/readiness require the nullable marker at `server/src/schema.sql:121` and `server/src/readiness.js:165-168`. Claude independently inspected all boundaries, reran focused **6/6**, readiness and the full server suite, and reproduced all four named mutations at **3/6, 5/6, 5/6, and 3/6**. Remote `main` is `6001de8`; Railway lists deployment `59322268-02ec-413b-8ed6-14104bb0b5ac` as `SUCCESS`; its logs contain `schema applied` and `[operator] listening on 8080`; public health reports schema v8. The box remains open until Clint exercises the repaired legacy path.
- [ ] Owned Ask and specialist requests distinguish a study whose reading has not finished from a missing or unauthorized study before reserving model spend.
  - 📋 **Physical restored-study → Scholar packet prepared; runtime result remains open (Codex 2026-08-15 13:43 CDT).** `store/desktop-restored-study-field-test.md` turns the remaining `Romans 8:1-4` check into a deterministic two-stage run: Stage A reopens existing history without regeneration; Stage B sends exactly one Scholar question only after Cole's fresh approval because it may consume one paid Ask. The packet names the visible pass state, exact fail-closed error families, stop conditions, and a value-safe result record. Creating the packet did not run the app, reserve spend, or establish field readiness.
  - ⛔ **Physical automation blocked before Stage A; this is not an Operator failure (Codex 2026-08-15 14:00 CDT).** Computer Use produced no UI state and timed out/reset against the display name, installed-app enumeration, and exact bundle ID `com.base1520.theoperator`. A value-safe filesystem read confirms the installed 1.4.4 app exists, but no app surface was observed and no click, typing, history selection, regeneration, or Scholar request occurred. Stage A and Stage B remain **NOT RUN**; complete the packet manually or after Computer Use is healthy. No Ask was reserved and no field-readiness claim changed.
  - ✅ **Field-packet paid-Ask boundary contract AUDIT CONFIRMED (Codex 2026-08-15 14:29/16:13/16:23 CDT; Claude independent audit filed 2026-08-15).** `scripts/test-desktop-restored-study-field-test.mjs` pins Stage A's no-mint boundary, fresh action-time approval for exactly one potentially paid Stage B Ask, no automatic retry, current fail-closed UI/server copy, value-safe result fields, and the distinction between automation failure and an Operator verdict. Its original canonical control plus nine targeted mutations passed **10/10** both directly and through `npm run test:desktop-restored-study-field-test`; the current strengthened suite is **12/12**. Claude independently reproduced the control and mutated the packet document itself from `fresh action-time approval` to `approval`; the named approval contract failed closed. The guard remains directly wired into `mobile:store:check` and `test:release`; after the release verifier was changed to snapshot `release.sh` once per invocation, a fresh strengthened `npm run --ignore-scripts test:release` completed successfully at 16:23 CDT. A fresh post-reconciliation readiness run remains **174 passed · 1 warning · 4 failed**. This protects the packet only: Stage A and Stage B remain **NOT RUN**, no Ask was reserved, and the parent stays open.
  - ✅ **Field-packet exact command-step follow-up AUDIT CONFIRMED (Codex 2026-08-15 15:29/16:13/16:23 CDT; Claude independent audit filed 2026-08-15).** The wiring validator parses trimmed `&&` command steps and requires the field-packet test as one exact step in both `mobile:store:check` and `test:release`; a non-executing wrapper cannot satisfy the guard by containing the command as a substring. The focused canonical control plus eleven mutations pass **12/12** directly and through `npm run test:desktop-restored-study-field-test`; new `echo node scripts/test-desktop-restored-study-field-test.mjs` mutations fail closed at both command-chain seams. Claude independently reproduced the real-tree and isolated controls, replayed both requested `echo` mutations with the named refusal, and added a novel packet-document mutation that weakened `fresh action-time approval` to `approval` and failed closed. Codex then freshly confirmed the current exact-step source, both recorded hashes, and the named **12/12** control at 16:13 CDT. The applied source exactly matches the pre-validated artifact, and its reverse apply-check passes. A prior fresh `npm run mobile:store:check` exercised the 12/12 guard and intentionally exited 1 at the unchanged **174 passed · 1 warning · 4 failed** board; after the single-read verifier fix, a fresh strengthened `npm run --ignore-scripts test:release` completed successfully at 16:23 CDT. This is static contract evidence only: Stage A and Stage B remain **NOT RUN**, no Ask was reserved, and the parent stays open.
  - 🧪 **Desktop restored-study resume SERVER DEPLOYMENT + REGISTERED-ROUTE AUDIT CONFIRMED / INSTALLED UPDATER DORMANT + LOCAL APPLE VERIFIER UNHEALTHY / REVIEW/COMMIT + PROPERLY PACKAGED CROSS-HOST-VERIFIED BUILD/INSTALL/FIELD RETEST PENDING (Codex reconciliation, 2026-08-15 11:18 CDT).** Launch and saved-history restoration call the existing `plain-read` IPC with internal `resumeOnly: true`; main serves a matching verified local document and returns null for a missing remembered id or local-mode miss, concurrent callers are single-flighted, and the hosted done-frame id is retained/re-bound for Scholar and Ask. The hosted client translates the internal option to the server's exact `restoreOnly` JSON key and never serializes `resumeOnly`. Controls are desktop **25/25**, hosted transport **43/43**, and server **91/91**. The registered Express `POST /v1/read` handler returns `409 STUDY_RESTORE_UNAVAILABLE` with zero mint-boundary calls for a stale restore and reaches that boundary exactly once for deliberate false; body erasure in `/private/tmp/operator-route-handler.0Emc0K` fails exactly that restore property at **90/91**. Claude independently reproduced that control and mutation at 11:07 CDT, closing the route-test audit. Railway deployment `0973e6a3-1e7c-453d-90ca-d6a69eccb763` remains independently verified `SUCCESS`/running with healthy public identity, but the dirty CLI archive has no reproducible Git-source receipt. Claude's 11:07 filing reported that a clean arm64 reinstall and strict verification passed after the post-sign updater graft was removed. Fresh Codex measurement does not reproduce that result now, but it also proves the failure is not Operator-specific: `/usr/bin/codesign --verify --deep --strict --verbose=4` fails the installed app, both staged apps, and the known Apple-signed TextEdit control (`CSSMERR_TP_NOT_TRUSTED`), while `spctl` reports an internal Code Signing subsystem error. Installed and staged arm64 are byte-identical and omit `app-update.yml`; Intel contains the updater file. A separate resource/code-page/CMS audit found no missing or mismatched sealed resources, no mismatched executable page hashes, matching nested CDHashes, and valid detached main-CMS bindings. Therefore the current supported conclusion is **local trust evaluator unhealthy; Operator distribution signature unresolved**, not proven-invalid or proven-valid. The current installed app has a dormant updater and must not be distributed. The new local release guard fails closed on both missing updater configuration and an unhealthy verifier; a properly packaged build must be verified on a clean supported macOS host, then after install, before promotion. Reviewed/committed source lineage and the physical Library → Scholar retest also remain open. Public GitHub v1.4.4 is unchanged. The parent stays unchecked.
  - 🧪 **H3 field-contract truth correction/source review LOCAL; Claude audit pending (Codex, 2026-08-16 07:08 CDT).** Independent read-only review held the green **12/0** packet because it falsely called Stage A “no new model request” even though `resumeOnly` may finish already-reserved provider work; targeted dated installed `1.4.4` without any receipt binding the current uncommitted restore/grounding source to those bytes; treated Scholar/input visibility as binding proof; omitted the exact `Study attached`/`Speaking generally` badge; let any plausible non-empty Romans answer become a false pass; and retained the retired finished-study refusal in its source basis. The corrected packet is explicitly blocked before Stage A until a non-secret installed-candidate/source/server receipt exists, separately approval-gates Stage A's possible completion spend and Stage B's one potentially paid Ask, requires `Study attached · Romans 8:1-4` before and after the request, records candidate/server identity, and treats general mode as failure. The guard now executes source truth for the resume/provider and badge boundaries, rejects non-executing wrappers plus comment/early-exit aggregate-script bypasses, and passes **20/0**; desktop resume remains **25/0**. The legacy Romans entry may still lack a finished binding because released `1.4.4` deleted it and a verified-cache return cannot reconstruct it; that is a precise runtime blocker, not a reason to regenerate or adopt untrusted bytes. H3's static packet/pins are locally source-reviewed, but Stage A and Stage B remain **NOT RUN**, no app was launched, no provider/Ask spend occurred, and the parent stays open.
  - ✅ **Local fix AUDIT CONFIRMED by Claude at 13:16 CDT; deployment independently verified by Codex at 13:43 CDT; field retest pending.** `server/src/study-ai-access.js:30-55` keeps ownership inside the query, deliberately permits an owned null document to reach application logic, returns `409 STUDY_READING_REQUIRED` with actionable open-and-finish-reading copy, and preserves the non-oracular `404 STUDY_NOT_FOUND` for missing or unowned IDs. `/v1/ask` and `/v1/sermon-assist` invoke the shared gate before `meter.reserveAsk`; both clients already surface the server message. Claude independently inspected the boundaries, reproduced focused **5/5**, full server exit 0, and all three named mutations. Claude also demonstrated that the route-order test is a textual tripwire: a call-text-preserving dead-code bypass stays green. Codex independently reproduced that limitation at **7/7**; it is recorded rather than misrepresented as behavioral route proof. Commit `6001de8` is remote, Railway deployment `59322268-02ec-413b-8ed6-14104bb0b5ac` is `SUCCESS`, and the public Ask/specialist probes are deployed and below HTTP 500. Those unauthenticated probes do not prove the owned unfinished-study behavior; Clint's field path remains open.
- [ ] Owned commentary retrieval distinguishes a study whose reading has not finished from a missing or unauthorized study before querying the commentary library.
  - ✅ **Local fix AUDIT CONFIRMED by Claude at 13:41 CDT and deployed; field retest pending.** `server/src/study-commentary.js` behaviorally drives the actual shared SQL gate before retrieval; the live route mounts that tested handler. An owned null document returns actionable `409 STUDY_READING_REQUIRED`, missing/unowned stays non-oracular `404 STUDY_NOT_FOUND`, and successful retrieval uses only the gated server row's reference and analysis. The existing mobile error path renders the server message. Codex's focused control is freshly **9/9** and the complete server suite previously exited 0. Printed isolated mutations accepting null documents, restoring the SQL null filter, preserving dead gate-call text while bypassing its execution, and omitting the selected reference fail at **6/9, 8/9, 7/9, and 8/9**. Claude independently reproduced the strongest dead-gate and missing-reference mutations at **7/9** and **8/9**, closing the prior textual-route hardening gap. Commit `6001de8` is remote, Railway deployment `59322268-02ec-413b-8ed6-14104bb0b5ac` is `SUCCESS`, and the public commentary route is deployed and below HTTP 500. That unauthenticated probe does not prove the owned unfinished-study behavior; Clint's field retest remains open.
- [ ] Rate, cost, and concurrency ceilings fail closed.
  - ✅ **Cost** is done and independently proven: the `daily_ceiling_usd` parser and both request gates were mutation-tested by Claude on 2026-08-11 (7 mutations, each caught by the correct assertion).
  - ⛔ **Concurrency implementation gap identified 2026-08-12.** The backend atomically caps daily/allowance counts and includes simultaneous reservations in the dollar brake, but no explicit global, per-account, or per-install maximum for currently executing model requests exists in tracked source. Cost visibility is not a concurrency ceiling. Define the intended limits, enforce them with durable or process-safe claims, and mutation-prove simultaneous acquire/refuse/release behavior before checking this row.
  - ⛔ **Short-window request-rate ceiling is source-audited absent (Codex, 2026-08-15).** All six model-spending entry points (`/v1/analyze`, `/v1/quick-study`, `/v1/guided-study`, `/v1/read`, `/v1/ask`, and `/v1/sermon-assist`) reach a model through study or ask reservations, but those guards provide lifetime/monthly/24-hour usage quotas and a global dollar brake—not a requests-per-short-window ceiling. The only explicit one-minute/hour rate controls in tracked server source protect registration and recovery; the two in-process Quick/Guided maps only coalesce identical work. Before implementation, Cole/product must select authenticated and anonymous identity scopes, window and burst policy, distributed atomic admit/refuse semantics, `Retry-After`, and fail-closed behavior; then every spending route needs concurrent-burst, boundary/reset, identity-isolation, and limiter-outage proof. This parent box stays open until both rate and concurrency controls are independently proven.
- [ ] Database backup and rollback evidence exists.

## Submission boundary

Do not upload a binary, create paid products, change production release stage, publish legal pages, or submit for review without Cole's action-time approval. Save screenshots and receipts from every external action in the release ledger.
